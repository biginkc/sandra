import { describe, it, expect } from "vitest";

describe("oauth-secret", () => {
  it.todo("OAuthSecret.toJSON returns '[REDACTED]'");
  it.todo("OAuthSecret.toString returns '[REDACTED]'");
  it.todo("JSON.stringify({ token }) does not leak the value");
  it.todo("reveal() returns the underlying value");
});
