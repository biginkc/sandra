import { describe, it, expect } from "vitest";

describe("settings/integrations form", () => {
  it.todo("renders Connect Slack button when no slack tokens row exists");
  it.todo("renders Disconnect button when slack row present");
  it.todo("toggling Slack pref calls setChannelEnabled action");
  it.todo(
    "renders Connect Google + timezone dropdown when google token absent",
  );
});
