# Pre-call setup status

## Candidate and evidence boundary

Draft PR #519: https://github.com/biginkc/sandra/pull/519
Branch `coach/precall-setup`, based on merged PR #515 (`8c7053e7024433f46791eac1b186c1b7a7cf10ec`).
The post-review working-tree candidate has now passed the private native gate: 12 atomic tests, disposable PostgreSQL 17 rehearsal, typecheck, 4,218 unit tests, and 1,253 RTL tests. The private rehearsal database was stopped afterward. The full synthetic Playwright suite also passes 89/89, including the coach-audio and browser-boundary fixtures that failed on the earlier pushed head.

Focused checks remain green: 67 provider, 11 draft/auth, and 4 panel tests (82 total), plus typecheck. The candidate is still uncommitted; these results are evidence for the pending frozen commit, not an approval.

## Completed implementation and checks

- Read-only preparation, typed setup snapshot, four-group accordion, scoped drafts, trusted file number and all six selectors implemented.
- Approved script JSON remains unchanged; no All openers or imported inbound paragraph; intentional two-part greeting remains separated.
- 517 independent frozen-source matrix tests: 160 individual choices, 128 opener/occupancy cases, 64 offer/closing cases, and 165 value pairs exercised across eight profiles. Total 86,944 section assertions.
- Combined browser matrix 15/15 passed: eight live profiles, 200 deterministic ordinary disruption cycles across two seeds, four viewport/keyboard cases. Additional deferred A→B→A/context retry and setup contrast cases passed; live contrast 14/14 passed.
- Four negative controls detected the intended failures; source restoration and green reruns verified.
- Browser review found dropdown layering and nested scrolling/offscreen Call defects; fixes and affected checks passed. Collapse retention, late draft resurrection, and absentee-to-tenant inference were also corrected.

## Review

Plan review: four requests, three substantive verdicts (two CHANGES_REQUIRED, one APPROVE); one malformed response excluded. Authenticated model metadata records Claude Fable 5.1.
Code review: one request, one substantive CHANGES_REQUIRED verdict, zero approvals, tied to commit 9c96379e. See FABLE-CODE.md and evidence/precall-fable-code-1.json.
Independent manual review remains pending. The coordinator controls merge admission; the Messages production hotfix currently has priority over new shared CI/browser/Fable dispatch.

## Release-blocking work

- Freeze the verified working tree, obtain independent review, renewed exact-commit Fable approval, and green CI.
- Remaining failure-injection coverage, 200% zoom and authenticated preview verification.
- Actual training calls: blocked on all-service cost ceiling and independently enforced 300-second termination.
- Production deployment identity, served behavior, four production training calls, final Fable assessment.

Paid attempts: **0**. Spend: **$0**. Hard ceilings: 24 attempts, $20 aggregate, 300 seconds per call; stop at the first limit. Only the dedicated training destination is authorized. It has no lead ID; file number must remain unavailable. No fabricated identity or real homeowner calls.

## Evidence

COVERAGE.json distinguishes verified commit evidence from later focused checks. Browser stress ledgers, review receipt, and focused/negative-control logs are retained in evidence/. These results do not claim exhaustive failure coverage or release approval.
