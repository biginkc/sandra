# My Leads provisional PRD acceptance receipt

**Status: provisional and incomplete.** This matrix records the available local evidence for PRD v0.2. It is not a release receipt and does not authorize a rollout.

**Candidate:** Sandra implementation is committed through `b4f48fdd`, paired with Jitter `c41f2e0`. Subsequent readiness-review changes are recorded in DEPLOYMENT-READINESS.md. Local acceptance is complete only to the scopes below; deployment and hosted migration execution are prohibited for this task.

**Evidence boundary:** `PASS (local)` means a recorded unit, RTL, or private PostgreSQL/loopback result. It does not prove deployed parity or live provider transport. `PARTIAL` means that one or more acceptance dimensions passed while a required browser, fresh replay, or live/release dimension remains. `PENDING` means the required evidence is not complete.

## Recorded evidence

- Final bounded full unit run passed **344 files / 3,778 tests**, and final bounded full RTL run passed **118 files / 1,265 tests** (`docs/my-leads/plan/LOCAL-ACCEPTANCE.md:58-65`). An earlier full RTL invocation had one unchanged campaign-form timeout; that historical result remains recorded accurately and is superseded for current readiness by the successful rerun. The client refresh regression passed **1 test** in `src/app/(dashboard)/my-leads/client.test.tsx:163-216`.
- Private timing proof passed six Central working-time deadline vectors, fractional minutes, invalid duration, and grant isolation (`scripts/verify-acquisition-time.mjs:21-41`; `LOCAL-ACCEPTANCE.md:23-24`).
- Private call-evidence proof covers bind-without-attempt, late original-rep attribution, replay, new-owner clock preservation, service-only start evidence, both reconciliation arrival orders, and no duplicate attempts (`scripts/verify-acquisition-evidence.mjs:54-191`; `LOCAL-ACCEPTANCE.md:41-42`). The signed local HTTP receiver then passed invalid-signature/operator-setup/unbound-context rejection, valid seller evidence, duplicate delivery, one-attempt, and original-assignee-clock checks (`LOCAL-ACCEPTANCE.md:58-62`). This is isolated local evidence, not live Jitter/Telnyx deployment evidence.
- The initial isolated full-schema verifier replayed the 216 baseline migrations and 13 feature migrations and passed launch/workflow checks at its checkpoint (`LOCAL-ACCEPTANCE.md:20-25`; `scripts/verify-my-leads-full-schema.mjs:51-99`). Because later function and launch revisions followed that checkpoint, it was superseded by the fresh replay below.
- The fresh candidate replay passed all **216 baseline + 13 feature migrations**, with all 13 source hashes matching the receipt immediately before browser acceptance (`LOCAL-ACCEPTANCE.md:45-48`).
- Browser evidence is bounded to the dedicated local lane in `e2e/my-leads.local.spec.ts:187-373`. After the required locator, recipient/detail-refresh, designation, keyboard, reporting-period, and warning-clearance checks were added, the final expanded serial lane passed all **7 tests in 12.4 seconds with zero retries** against the production build. It covered member self-scope/layout, owner selected-rep plus foreign denial, designation off/on, keyboard dialog cancellation, Under Contract period persistence, Contacted warning visibility, the 106 workflow, and the 107 note/appointment/handoff workflow. The preceding four-test lane and fresh replay are retained in `LOCAL-ACCEPTANCE.md:45-54`; the final expanded result is the current local browser result.
- The final local outcome proof reports **exactly four principals, exactly one deliberate appointment, zero sequence enrollments, and configured-owner Needs sequence handoff** (`LOCAL-ACCEPTANCE.md:60-62`).
- The local campaign uses exactly four fixed principals and nine synthetic leads; additional temporary CI accounts and rerun replacements are now explicitly authorized, with exact-run cleanup (`LOCAL-ACCEPTANCE.md:20-22`).

## Acceptance matrix

| # | PRD acceptance item | Exact evidence and scope | Status |
|---:|---|---|---|
| 1 | Owner toggles Acquisitions independently; only new assignments get eligible timing. | Settings wrappers cover designation/settings CAS and malformed results (`src/lib/my-leads/settings.test.ts:24-127`). The private launch verifier covers owner designation, roster, settings, and designation invalidation (`scripts/verify-my-leads-launch.mjs:104-145`, `228-249`). The expanded browser lane passed the real owner off/on controls and restored the initial designation (`e2e/my-leads.local.spec.ts:235-250`). | **PASS (local/browser)** |
| 2 | Owner inspects Maria’s queue/KPIs; rep results match; unauthorized and foreign selection is rejected. | Query wrappers test member self-scope, owner-selected member, Central range normalization, and read failures (`src/lib/my-leads/queries.test.ts:12-51`). Private evidence checks member/cursor/viewer bindings and original-rep KPI attribution (`scripts/verify-acquisition-evidence.mjs:79-118`). The owner/foreign browser case passed (`e2e/my-leads.local.spec.ts:207-233`), and the expanded lane passed; same selected rep/period KPI equality passed in the expanded nine-test readiness browser lane. | **PARTIAL** |
| 3 | Exactly five sections, in order; no Follow-up or Won. | Component test asserts all five labels and six KPI tiles (`src/app/(dashboard)/my-leads/_components/queue.test.tsx:88-103`). Independent per-stage paging and permitted action coverage are also tested (`src/app/(dashboard)/my-leads/_components/queue.test.tsx:105-177`). The expanded seven-test browser lane passed (`e2e/my-leads.local.spec.ts:187-373`). | **PASS (local/browser)** |
| 4 | Call dialog does not stop the clock; actual seller initiation does; unanswered calls and optional DialPad recording are handled without duplicates. | Pure warning tests separate actual first-call evidence from outreach (`src/lib/my-leads/time.test.ts:42-54`). Call evidence and reconciliation proofs cover service-only actual-start evidence, manual outreach versus external-call timing, replay, and dedupe (`scripts/verify-acquisition-evidence.mjs:120-168`). Signed local HTTP receiver proof passed valid seller evidence and duplicate delivery checks (`LOCAL-ACCEPTANCE.md:58-62`). Live provider/deployed producer parity is still open. | **PARTIAL** |
| 5 | First outreach reaches Contacted without regressing advanced stages; non-call outreach does not fabricate a first call. | Full-schema/manual workflow checks and the expanded browser workflow exercise attempt-to-Contacted (`scripts/verify-my-leads-full-schema.mjs:84-99`; `e2e/my-leads.local.spec.ts:279-329`, `331-343`; `LOCAL-ACCEPTANCE.md:47-54`). The separate live seller-leg initiation gate remains open under item 4. | **PASS (local/browser)** |
| 6 | Readiness is manual; motivation is required; explicit No motivation provided is valid; no appointment is required. | Dialog tests cover missing motivation, explicit no-motivation, and independent temperature (`src/app/(dashboard)/my-leads/_components/dialogs.test.tsx:20-64`). The expanded browser workflow passed the required validation and readiness journey (`e2e/my-leads.local.spec.ts:293-302`). | **PASS (local/browser)** |
| 7 | Offer and signed contract can be recorded during the first call; offer follow-up is required and nothing is sent. | Dialog validation covers positive integer cents, methods, required follow-up, and no send (`src/app/(dashboard)/my-leads/_components/dialogs.test.tsx:66-98`). Private launch workflow covers offer, contract, and archive (`scripts/verify-my-leads-launch.mjs:399-418`); full-schema checks include offer and contract RPCs (`scripts/verify-my-leads-full-schema.mjs:88-97`). The expanded browser workflow passed the offer/contract path (`e2e/my-leads.local.spec.ts:304-321`). The live provider gate and release remain separate. | **PASS (local/browser)** |
| 8 | Contract becomes Under Contract, survives date-range changes, and is deliberately archived. | Lifecycle dialog payloads are tested (`src/app/(dashboard)/my-leads/_components/dialogs.test.tsx:166-230`); private workflow verifies contract then archive (`scripts/verify-my-leads-launch.mjs:407-418`); the expanded browser lane passed the Under Contract period-persistence and contract/archive journeys (`e2e/my-leads.local.spec.ts:268-277`, `279-329`). | **PASS (local/browser)** |
| 9 | Decline and handoff apply Offer Declined/Needs sequence/reassignment, exit the queue, and create no automatic work. | Private workflow checks decline state and recipient reassignment plus stale handoff rejection (`scripts/verify-my-leads-launch.mjs:428-490`). The repaired member handoff passed in the expanded browser lane (`e2e/my-leads.local.spec.ts:331-373`; `LOCAL-ACCEPTANCE.md:52-54`). The final local outcome proof records exactly one deliberate appointment, zero sequence enrollments, and configured-owner handoff (`LOCAL-ACCEPTANCE.md:60-62`). | **PASS (local/browser)** |
| 10 | Central 30-working-minute clock, Friday-to-Monday carryover, after-hours, DST, and no login effect. | TypeScript and private PostgreSQL timing vectors cover Central windows, weekends, fractional minutes, and spring/fall DST (`src/lib/my-leads/time.test.ts:12-25`; `scripts/verify-acquisition-time.mjs:21-41`). Browser display crossing the boundaries has not been accepted. | **PARTIAL** |
| 11 | Contacted with no future valid appointment/callback is immediately red; a deliberate future next step clears it; nothing schedules automatically. | Pure warning transitions require a strictly future next step and reject equal/past values (`src/lib/my-leads/time.test.ts:28-35`). Existing appointment/callback bindings reuse canonical controls (`src/app/(dashboard)/my-leads/_components/existing-detail-actions.test.tsx:24-51`). The expanded browser lane passed the 102 warning visibility, keyboard cancellation, and 107 warning clearance after deliberate booking (`e2e/my-leads.local.spec.ts:252-277`, `331-365`). | **PASS (local/browser)** |
| 12 | Needs offer warns at 12 elapsed hours; Offer Sent warns after required follow-up. | Pure warning tests independently cover the elapsed offer threshold and follow-up instant (`src/lib/my-leads/time.test.ts:37-54`). The readiness browser lane now verifies normal pre/post deadline states and red indicators for both offer categories; exact thresholds are independently tested in the timing functions. | **PARTIAL** |
| 13 | Stale count is current, deduplicated, and period-independent; original-rep attribution and unknown/pending handling are preserved. | Query and KPI private evidence covers original appointment/call credit, reschedule denominator, owner isolation, and manual-versus-call timing (`scripts/verify-acquisition-evidence.mjs:98-138`; `src/lib/my-leads/queries.test.ts:16-28`). Independent raw-fact aggregate comparison, period-independent positive stale count, and authenticated owner/self KPI tile equality now pass in the readiness lane. | **PARTIAL** |
| 14 | Maria’s launch cohort is Contacted or retains later milestones, with no fake activity, regression, resurrection, or first-call KPI inclusion. | The fresh full-schema replay covers a genuine pre-feature lead with no episode, preview/apply/replay, no fictitious activity, and launch initialization (`scripts/verify-my-leads-full-schema.mjs:59-80`; `LOCAL-ACCEPTANCE.md:45-48`). Launch rehearsal also covers exact cohort/replay/rollback and stale fingerprint/designation cases (`scripts/verify-my-leads-launch.mjs:146-244`). | **PASS (local)** |
| 15 | Existing Leads, notes, appointments, calling, eSign, assignment guards, and DNC protections retain behavior; no blanket reverse synchronization. | Full unit suite passed as recorded above. My Leads detail tests verify bounded lazy groups, retry, note composer, and existing appointment/callback controls (`src/app/(dashboard)/my-leads/_components/detail-panel.test.tsx:16-92`; `existing-detail-actions.test.tsx:24-51`). This does not replace the required final neighbor smoke, CI, or deployment/migration verification. | **PARTIAL** |
| 16 | Known-fixture queue/KPI math and authenticated UI, owner selector, dialogs, warnings, narrow screen, zoom, and keyboard behavior are verified under release process. | Final unit/RTL, fresh 216+13 replay, production build, signed local receiver, and all seven serial browser tests provide local evidence (`docs/my-leads/plan/LOCAL-ACCEPTANCE.md:45-64`; `e2e/my-leads.local.spec.ts:187-373`). Keyboard, reporting-period persistence, warning visibility/clearance, and narrow layout are covered. Native CI, required migration/deployment verification, real Maria data, live provider contract, remain open. Native 200% Chromium tab zoom also passed via `scripts/verify-my-leads-local-zoom.mjs`; root inspected the viewport screenshot. | **PARTIAL** |

## Release gates still open

1. Obtain exact-candidate independent review/admission, then main CI and required migration/deployment verification. Local build, tests, and docs do not establish release completion (`docs/my-leads/plan/TEST-AND-RELEASE.md:104-124`).
2. Verify the actual Jitter seller-leg initiation producer/receiver contract under the existing provider limits. Signed local HTTP receiver proof is a local contract check, not live transport evidence (`docs/my-leads/CONTRACTS.md:102-126`; `docs/my-leads/plan/LOCAL-ACCEPTANCE.md:58-62`).
3. Exercise the feature against real Maria/production-like data and complete the admitted cohort/rollout process; the four-principal synthetic campaign is not that proof (`docs/my-leads/PRD.md:176-183`; `docs/my-leads/plan/LOCAL-ACCEPTANCE.md:20-22`).
4. Native 200% zoom is closed: the task-owned Chromium extension set actual tab zoom to 2.0; overflow and dialog interaction checks passed. Keyboard, report-period persistence, warning clearance, designation, no-automatic-work, production build, and signed local HTTP checks are also closed by the recorded local tests.

Until these gates close, this receipt should be read as a bounded readiness record rather than an acceptance or deployment approval.

## Readiness review addendum

The later nine-test local browser run, current build and fresh SQL evidence supersede the earlier seven-test checkpoint where noted above. See LOCAL-ACCEPTANCE.md and DEPLOYMENT-READINESS.md for exact candidate checks. Real provider/deployment/migration/production-cohort checks remain intentionally unexecuted under the user's deployment-readiness-only scope.
