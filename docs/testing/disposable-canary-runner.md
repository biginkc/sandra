# Disposable canary validation (candidate)

Run `node scripts/run-disposable-canaries.mjs` with Node, Docker, Supabase CLI 2.116.0, and installed repository dependencies. The manual GitHub workflow provisions the same prerequisites. No hosted database or provider secrets are required.

The runner creates a unique temporary Supabase project, replays this checkout's migrations, selects the existing AI dispatch, Sendillo inbound, Dialpad inbound/STOP, and sequence tick/queue integration suites, then destroys its own project without retaining a database backup. A failed test or setup returns nonzero; cleanup failure also returns nonzero and preserves the temporary directory for investigation. Use a dedicated Docker runtime locally; `SANDRA_CANARY_DOCKER_HOST` may select a Unix socket. Local API/SQL ports 54321/54322 must be available.

These are application regression checks with model stubs and the existing mock messaging provider. They do not prove live provider credentials, external delivery, production routing, or production availability. The fetch guard permits only the loopback API and rejects redirects; it is not a kernel-level network firewall.

The corrected local run passed all 108 tests across the five selected suites and destroyed the owned database. Independent manual source review passed; separate final code approval is pending. Hosted execution of this new workflow has not been certified. It does not remove the old four scripts, change their schedules, finalize coverage, or authorize production mutations. The proposed eventual schedule and separate production-monitoring contract require the user's pending placement decision. Exhaustive Messages/My Leads coverage remains a later discussion.

Initial validation: 101 of 108 integration cases passed; seven failed because the fresh database had no canonical owner. The runner now seeds a namespaced owner through Auth and the normal membership guard, preserving production guard behavior. The first failed stack was destroyed. A subsequent script-format failure also cleaned up successfully. Signal tests for startup/test cancellation passed, including a child that ignores SIGTERM. The third setup attempt rejected an invalid custom run namespace and cleaned up. The bootstrap now uses the existing identity constructors directly. The fourth attempt passed all 108 integration tests with successful teardown.

Final evidence: `/tmp/sandra-disposable-canary-fourth-run.log` (exit 0, 108 passed); `/tmp/sandra-disposable-typecheck-v3.log` (exit 0); focused ESLint passed. Docker inventory after teardown contained zero containers and zero volumes in the dedicated task runtime. Cancellation tests use fake command executables and do not prove actual Docker teardown on interruption. SIGKILL, host loss, or an external cancellation deadline can still prevent cleanup; default Docker context must itself be local unless the explicit Unix-socket option is supplied.
