---
phase: 04-tasks-integrations-v2-slack-google-calendar
plan: 10
type: execute
wave: 6
depends_on: [07, 08, 09]
files_modified:
  - src/app/(dashboard)/messages/dispo-actions.ts
  - src/lib/tasks/index.ts
  - src/app/(dashboard)/messages/dispo-actions.integration.test.ts
autonomous: false
requirements: [SLACK-01, CAL-01, INTEG-01]

must_haves:
  truths:
    - "setOutreachDispo schedules dispatchSlackTaskAssigned + dispatchTaskCalendarEvent in a single after() block when a task is created with assignee_id != actor_id"
    - "Updating tasks.due_at calls dispatchTaskCalendarEventUpdate in after()"
    - "loadIntegrationPrefs is called ONCE before the after() block; the after() body reads the captured prefs object"
    - "All three dispatchers (existing dispatchTaskAssigned + new Slack + new Calendar) use Promise.allSettled — one failure does not abort the others"
    - "The user-visible dispo write completes in <200ms regardless of integration outage"
    - "Integration smoke test asserts a task created via setOutreachDispo schedules the after() callback that invokes Slack + Calendar dispatchers (mocked)"
    - "End-to-end manual smoke verified by Jarrad: connect Slack, connect Google, trigger callback dispo on a test property, observe DM lands, observe event lands, click Mark Done, observe task completes + message updates"
  artifacts:
    - path: "src/app/(dashboard)/messages/dispo-actions.ts"
      provides: "setOutreachDispo modified to fan out via after()"
    - path: "src/lib/tasks/index.ts"
      provides: "Optionally extends updateTaskDueAt to schedule Calendar update"
  key_links:
    - from: "src/app/(dashboard)/messages/dispo-actions.ts"
      to: "src/lib/integrations/slack/dispatch.ts + src/lib/integrations/google/dispatch.ts"
      via: "after(() => Promise.allSettled([dispatchTaskAssigned, dispatchSlackTaskAssigned, dispatchTaskCalendarEvent]))"
      pattern: "Promise.allSettled\\(\\["
---

<objective>
Wire Slack + Calendar dispatchers into the live dispo flow. This is the final integration point: when `setOutreachDispo` creates a task with `assignee_id !== actor_id`, the same `after()` block that fires the existing in-app `dispatchTaskAssigned` ALSO fires `dispatchSlackTaskAssigned` and `dispatchTaskCalendarEvent`. When `tasks.due_at` is updated, `dispatchTaskCalendarEventUpdate` runs in after().

Includes the end-of-phase manual smoke checkpoint per CONTEXT.md success criteria #4 ("Existing in-app dashboard panel + bell notification continue to work — the Slack and Calendar paths are additive, not replacements") and #5 (CI green).

Output: 1 callsite modification, optional helper extension, 1 integration test, plus a human-verify checkpoint.
</objective>

<execution_context>
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-CONTEXT.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-RESEARCH.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-PATTERNS.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-02-SUMMARY.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-03-SUMMARY.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-07-SUMMARY.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-08-SUMMARY.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-09-SUMMARY.md
@src/app/(dashboard)/messages/dispo-actions.ts
@src/lib/notifications/dispatch.ts
@src/lib/integrations/slack/dispatch.ts
@src/lib/integrations/google/dispatch.ts
@src/lib/integrations/prefs.ts
@src/lib/tasks/index.ts
@src/lib/skip-trace/actions.ts
@AGENTS.md

<gotchas>
- AGENTS.md: Sandra is on Next 16. `after()` is imported from `next/server` (NOT `next/cache`). Stable API since 15.1. Read `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/after.md` if any signature surprises arise.
- Pattern 7 in 04-RESEARCH.md (lines 624-647) shows the exact wiring: load prefs ONCE before the after() block; pass the captured object into the body so cookies/headers aren't accessed inside after() (which runs after the request lifecycle).
- Promise.allSettled (NOT Promise.all): one failed dispatcher must not abort the others. The existing dispatchTaskAssigned in dispatch.ts already returns its own ok/sent flag; pair behavior aligned.
- The deepLink for Slack/Calendar payloads should use the prod URL: `https://sandra-sooty.vercel.app/messages?property_id=${propertyId}` (per project memory, this is the stable alias). Locally / in CI, NEXT_PUBLIC_APP_URL or APP_URL env var.
- `humanDueDate(dueAt, timezone)` from `src/lib/notifications/format.ts` should be the source of the dueLabel for the Slack message — same string the in-app bell shows.
- For the due_at update path: read `src/lib/tasks/index.ts` to see if there's already an update path. If not, this plan does NOT add one — it documents that updating due_at is a V3 ergonomics improvement and gates the calendar update behind a future task-edit flow. The CAL-01 acceptance criterion only requires events to land on create AND update — but in the V1 substrate (PR #112), there's no UI surface for editing due_at. So for V2, the create path is the primary deliverable; the update path is wired in the same dispatcher module but called from wherever a future due_at edit lands.

  CONFIRM during execution: grep `src/lib/tasks/index.ts` for an `updateTaskDueAt` or similar; if absent, document in the SUMMARY that update is callable from dispatchTaskCalendarEventUpdate but no caller exists yet. The V2 acceptance is "lands on create" + "update path exists for when V3 ships".
</gotchas>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire dispatchers into setOutreachDispo via after()</name>
  <files>
    src/app/(dashboard)/messages/dispo-actions.ts,
    src/app/(dashboard)/messages/dispo-actions.integration.test.ts
  </files>
  <read_first>
    - src/app/(dashboard)/messages/dispo-actions.ts (entire file — current setOutreachDispo flow; find the lines where dispatchTaskAssigned is currently called for the in-app bell)
    - src/lib/notifications/dispatch.ts (dispatchTaskAssigned signature — same args we'll pass to the new dispatchers)
    - src/lib/skip-trace/actions.ts (existing after() pattern — lines 184-190 + 296)
    - src/lib/integrations/slack/dispatch.ts (Plan 07 — dispatchTaskAssignedSlack signature)
    - src/lib/integrations/google/dispatch.ts (Plan 08 — dispatchTaskCalendarEvent signature)
    - src/lib/integrations/prefs.ts (Plan 03 — loadIntegrationPrefs)
    - src/lib/notifications/format.ts (humanDueDate)
    - .planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-RESEARCH.md (Pattern 7 lines 624-647 — exact wiring)
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/after.md (Sandra is on Next 16; confirm `after` import path is `next/server`)
  </read_first>
  <behavior>
    Before this plan: setOutreachDispo only fires `dispatchTaskAssigned` (in-app bell) when a task is created with assignee_id !== actor_id. Already fire-and-forget per existing pattern.

    After this plan:
    - When task assigned to a different user, setOutreachDispo:
      1. Computes the dispatch input ONCE (taskTitle, taskType, dueAt, propertyAddress, propertyId, deepLink) BEFORE the after() block
      2. Loads integration prefs ONCE before after() — `const prefs = await loadIntegrationPrefs(supabase, resolvedAssignee);`
      3. Schedules a single after() that runs:
         ```typescript
         await Promise.allSettled([
           dispatchTaskAssigned(supabase, { /* existing args */ }),
           dispatchTaskAssignedSlack({
             taskId, assigneeId: resolvedAssignee, taskTitle, taskType, dueAt,
             propertyAddress, deepLink, timezone: prefs.timezone,
           }),
           dispatchTaskCalendarEvent({
             taskId, assigneeId: resolvedAssignee, taskTitle,
             propertyAddress, dueAt, timezone: prefs.timezone, deepLink,
           }),
         ]);
         ```
      4. Returns the existing setOutreachDispo result (no waiting).

    The deepLink construction:
    - `const appUrl = process.env.NEXT_PUBLIC_APP_URL ?? process.env.APP_URL ?? "https://sandra-sooty.vercel.app";`
    - `const deepLink = \`\${appUrl}/messages?property_id=\${propertyId}\`;`

    Imports added (top of file):
    ```typescript
    import { after } from "next/server";  // confirm — may already be imported
    import { dispatchTaskAssignedSlack } from "@/lib/integrations/slack/dispatch";
    import { dispatchTaskCalendarEvent } from "@/lib/integrations/google/dispatch";
    import { loadIntegrationPrefs } from "@/lib/integrations/prefs";
    ```

    Existing dispatchTaskAssigned call moves INTO the Promise.allSettled. No duplicate dispatch — the in-app bell continues to fire from the same after() block.

    The integration test (dispo-actions.integration.test.ts):
    - Creates a test user (actor) and a different test user (assignee)
    - Creates a property + conversation
    - Mocks @/lib/integrations/slack/dispatch and @/lib/integrations/google/dispatch (with vi.mock — partial)
    - Calls setOutreachDispo with assignee_id !== actor.id and a task-creating dispo (callback_requested)
    - Awaits the test framework's "flush after()" pattern (vitest helper or `await new Promise(res => setTimeout(res, 50))`)
    - Asserts the mocked dispatchTaskAssignedSlack and dispatchTaskCalendarEvent were called with the expected shape
    - Asserts the mocked in-app dispatchTaskAssigned was ALSO called (Promise.allSettled — all three fire)
    - Cross-check: when actor === assignee (self-assign), NONE of the three dispatchers fire (existing self-assign suppression preserved)
  </behavior>
  <action>
    1. Open `src/app/(dashboard)/messages/dispo-actions.ts`. Find the existing `dispatchTaskAssigned` call site within `setOutreachDispo` (it sits inside an `if (resolvedAssignee !== user.id)` branch per the V1 substrate).

    2. Replace that single call with the after()-block fan-out:
       - Add the three new imports listed in &lt;behavior&gt;
       - Compute deepLink locally
       - Call `loadIntegrationPrefs(supabase, resolvedAssignee)` BEFORE the after() block, capture as `prefs`
       - Wrap the existing dispatchTaskAssigned call inside `after(async () => { await Promise.allSettled([...]); })` along with the two new dispatchers

    3. If the existing code already wraps dispatchTaskAssigned in `after(() => dispatchTaskAssigned(...))` from the V1 substrate, refactor that block to use Promise.allSettled with all three dispatchers. Don't add a SECOND after() — coalesce into one.

    4. Verify the existing self-assign suppression (resolvedAssignee !== user.id) still gates the entire block — Slack/Calendar dispatch should NOT fire when actor self-assigns, matching dispatchTaskAssigned semantics.

    5. Create `src/app/(dashboard)/messages/dispo-actions.integration.test.ts`. Read the existing integration tests in `src/lib/notifications/dispatch.integration.test.ts` for the bootstrap pattern (test users, env-gate, beforeAll setup). Mock the dispatchers with vi.mock so they don't try to call real Slack/Google APIs. Cover:
       - Task-creating dispo with assignee != actor → all three dispatchers called once with the expected shape
       - Task-creating dispo with assignee == actor → none of the three dispatchers called
       - When loadIntegrationPrefs throws (force via mock), the dispatchers still fire with default timezone (because loadIntegrationPrefs internally catches and returns defaults — Plan 03 contract)
       - When one dispatcher throws (force via mock), the other two still receive their calls (Promise.allSettled semantics)

    6. Run `npm run typecheck` + the integration test under `npm run test:integration`.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run 'src/app/(dashboard)/messages/dispo-actions.integration.test.ts' 2&gt;/dev/null || npm run test:integration -- 'src/app/(dashboard)/messages/dispo-actions.integration.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispatchTaskAssignedSlack' 'src/app/(dashboard)/messages/dispo-actions.ts'` returns at least 1
    - `grep -c 'dispatchTaskCalendarEvent' 'src/app/(dashboard)/messages/dispo-actions.ts'` returns at least 1
    - `grep -c 'loadIntegrationPrefs' 'src/app/(dashboard)/messages/dispo-actions.ts'` returns at least 1
    - `grep -cE 'Promise\.allSettled' 'src/app/(dashboard)/messages/dispo-actions.ts'` returns at least 1
    - `grep -cE 'after\(' 'src/app/(dashboard)/messages/dispo-actions.ts'` returns at least 1
    - `grep -cE 'next/server' 'src/app/(dashboard)/messages/dispo-actions.ts'` returns at least 1
    - The Slack + Calendar dispatch is gated by the SAME `if (resolvedAssignee !== user.id)` (or equivalent) check used by the existing dispatchTaskAssigned — verified by reading the diff
    - `grep -cE '^\s*it\(' 'src/app/(dashboard)/messages/dispo-actions.integration.test.ts'` returns at least 4
    - `npm run typecheck` exits 0
    - Integration test passes (either via npm test or npm run test:integration depending on the file's `.integration.test.` suffix)
  </acceptance_criteria>
  <done>setOutreachDispo fans out to all three dispatchers via a single after() block; integration test proves all three fire on assigned-to-other and zero fire on self-assign.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire calendar update path for due_at change (if updateTaskDueAt exists)</name>
  <files>src/lib/tasks/index.ts</files>
  <read_first>
    - src/lib/tasks/index.ts (entire file — confirm whether updateTaskDueAt or rescheduleTask exists; the V1 substrate may NOT have such a path)
    - src/lib/integrations/google/dispatch.ts (Plan 08 — dispatchTaskCalendarEventUpdate)
    - .planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-CONTEXT.md (D-07 — task complete is no-op, due_at change updates the event)
  </read_first>
  <behavior>
    Branch on what's in src/lib/tasks/index.ts:

    Path A — if a `rescheduleTask(supabase, { taskId, newDueAt })` or similar function EXISTS:
    - After the DB update, schedule `after(() => dispatchTaskCalendarEventUpdate({...}))` with the same input shape used by dispatchTaskCalendarEvent in Plan 10 Task 1.
    - Add a unit test asserting the dispatcher is called.

    Path B — if NO due_at update path exists in V1:
    - Document in the SUMMARY that this plan does NOT introduce one.
    - Add a comment near the calendar dispatcher import in dispo-actions.ts: `// Note: due_at edits trigger dispatchTaskCalendarEventUpdate from any future task-edit flow. V1 substrate (PR #112) does not expose due_at editing in the UI.`
    - The dispatchTaskCalendarEventUpdate function is still callable from any future caller — this plan creates the wiring contract, not the trigger.

    Path C — if `snoozeTask` exists (V1 substrate has it for the dashboard "Snooze 1d/3d/1w" actions):
    - snoozeTask DOES change due_at → wire the after() Calendar update right after snoozeTask's DB write.
    - This is small (3-5 LoC) and high-value (snooze in the dashboard now syncs the calendar).
    - Add unit test.
  </behavior>
  <action>
    1. Read `src/lib/tasks/index.ts`. Determine which of A/B/C above applies.

    2. **If Path C (snoozeTask exists):** modify snoozeTask to call dispatchTaskCalendarEventUpdate via after() AFTER the supabase update succeeds. Imports as needed. Use the same input shape as Plan 10 Task 1 (taskId, assigneeId, taskTitle, propertyAddress, dueAt, timezone, deepLink — fetched from the task row + a small property join + loadIntegrationPrefs).

       Add the corresponding unit test in `src/lib/tasks/index.test.ts` (file already exists per the directory listing). Mock dispatchTaskCalendarEventUpdate; assert it's called after snoozeTask succeeds.

    3. **If Path B (no due_at change path):** add the noted comment in dispo-actions.ts and write a clear paragraph in the SUMMARY explaining the deferral. NO code changes here. Do not invent an updateTaskDueAt function — out of scope for V2.

    4. **If Path A (some reschedule path exists):** wire it the same way as Path C. Same test pattern.

    Run `npm run typecheck` + the relevant test.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run src/lib/tasks/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - If Path C/A taken: `grep -c 'dispatchTaskCalendarEventUpdate' src/lib/tasks/index.ts` returns at least 1; `grep -c 'after(' src/lib/tasks/index.ts` returns at least 1
    - If Path B taken: 04-10-SUMMARY.md documents the deferral with rationale; src/app/(dashboard)/messages/dispo-actions.ts contains a comment referencing dispatchTaskCalendarEventUpdate as a future hook
    - `npm run typecheck` exits 0
    - `npm test -- --run src/lib/tasks/index.test.ts` exits 0
  </acceptance_criteria>
  <done>Calendar update path is either wired (preferred) or explicitly deferred with a documented contract. Phase still satisfies CAL-01 because event creation is the primary requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end manual smoke (Jarrad)</name>
  <what-built>
    Phase 04 Tasks Integrations is end-to-end wired: OAuth handshake routes (Plans 05/06), token storage (Plan 02), prefs/timezone (Plan 03), Slack DM + Mark Done webhook (Plan 07), Google Calendar event create (Plan 08), settings page (Plan 09), and the dispatch wire-up in setOutreachDispo (this plan).
  </what-built>
  <how-to-verify>
    Sandra requires a manual smoke for the Slack/Calendar paths because Playwright cannot fake a Slack workspace + Google account end-to-end.

    Pre-flight (Jarrad confirms):
    - Slack app exists at api.slack.com/apps with bot scopes chat:write, im:write, users:read, users:read.email; redirect URL https://sandra-sooty.vercel.app/api/oauth/slack/callback configured; Interactivity Request URL https://sandra-sooty.vercel.app/api/webhooks/slack/actions configured.
    - Google Cloud OAuth client exists with Calendar API enabled; redirect URI https://sandra-sooty.vercel.app/api/oauth/google/callback authorized.
    - Vercel prod env has SLACK_CLIENT_ID, SLACK_CLIENT_SECRET, SLACK_SIGNING_SECRET, GOOGLE_OAUTH_CLIENT_ID, GOOGLE_OAUTH_CLIENT_SECRET, GOOGLE_OAUTH_REDIRECT_URI, OAUTH_TOKEN_ENCRYPTION_KEY, OAUTH_STATE_SIGNING_SECRET.

    Smoke procedure (open URLs via `open` for Jarrad — single step at a time per global memory):

    1. `open https://sandra-sooty.vercel.app/settings/integrations` — expected: page loads, two cards (Slack + Google Calendar) both showing "Connect" buttons, timezone dropdown defaults to America/Chicago.
       - Jarrad: confirm or report what you see.

    2. Click "Connect Slack" — expected: redirect to Slack consent screen for the BMH workspace; approving redirects back to /settings/integrations?connected=slack with a green banner; Slack card now shows "Disconnect Slack" + an enabled-by-default toggle.
       - Jarrad: confirm or report.

    3. Click "Connect Google Calendar" — expected: redirect to Google consent (asking for Calendar Events + email); approving redirects back with ?connected=google banner; Google card shows your gmail email.
       - Jarrad: confirm or report.

    4. Trigger a callback dispo on a test property:
       - `open https://sandra-sooty.vercel.app/messages?status=open` then pick a non-prod-sensitive conversation
       - Open the dispo popover, choose "Callback requested", set due to "Tomorrow", assignee = the OTHER BMH user (NOT yourself)
       - Save
       - Expected: the OTHER user (whose Slack + Google are connected for THEIR account) receives the DM + calendar event.

    5. Check Slack: the assignee user gets a DM "Task assigned: ..." with property address, due label, and a green "Mark Done" button.
       - Jarrad: confirm.

    6. Check Google Calendar: a 30-min event "Follow up: <address>" lands on the assignee's primary calendar at the due time.
       - Jarrad: confirm.

    7. From the assignee's Slack, click "Mark Done":
       - Within 1-2s, the message updates to "✓ Marked done: <task title>"
       - Back in /messages, the conversation no longer has the open task badge
       - Jarrad: confirm.

    8. Sanity: the in-app bell notification still appears for the assignee (existing dashboard panel + bell unchanged) — CONTEXT.md success criterion #4.
       - Jarrad: confirm.

    9. Disconnect flow:
       - On /settings/integrations, click Disconnect Slack — expected: Slack card returns to Connect state; user_oauth_tokens row gone (verify via Supabase dashboard if curious; not required for sign-off)
       - Same for Google.
       - Jarrad: confirm.

    10. CI gate:
        - `gh run list --branch <branch> --limit 5`
        - typecheck + unit + RTL + Playwright golden paths all green per CONTEXT.md success criterion #5.
        - Jarrad: confirm.

    Reply "approved" if all 10 steps pass, OR describe the failure step-by-step. Failure modes:
    - Slack DM not received → check user_oauth_tokens has a bot row for the assignee; re-run Connect Slack on the assignee's account
    - Calendar event not received → check user_oauth_tokens has a google row; re-run Connect Google
    - Mark Done doesn't update message → check Slack interactivity Request URL is set + signing secret matches the env var
    - Connect Slack returns ?error=state → the OAUTH_STATE_SIGNING_SECRET env var differs between the start route and the callback route (shouldn't be possible — same Vercel deployment) OR the user took longer than 10 min between click and approval
  </how-to-verify>
  <action>This is a human verification checkpoint. Jarrad runs through the 10-step smoke procedure in &lt;how-to-verify&gt; against the prod deployment and replies with the resume-signal. No automated execution occurs in this task — Tasks 1 and 2 above complete the code wiring; this task gates phase sign-off behind a real Slack/Google round-trip that Playwright cannot fake.</action>
  <verify>User reply confirms each of the 10 steps passed (or identifies a specific failure step).</verify>
  <done>Jarrad has typed "approved" with all 10 smoke steps green, satisfying CONTEXT.md success criteria #4 (existing in-app paths still work) and #5 (CI green + manual smoke).</done>
  <resume-signal>Type "approved" or describe issues step-by-step</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispo-actions server action → after() | Captured prefs object crosses request lifecycle boundary; cookies/headers must NOT be re-read inside after(). |
| after() → external dispatchers | All three are best-effort; failure of one does not abort the others (Promise.allSettled). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Information Disclosure | task data in deep link | accept | The deep link points at /messages?property_id=… which only authenticated Sandra users can resolve. The id itself is opaque (UUID). |
| T-04-10-02 | Tampering | someone else's task fired into Slack | mitigate | Existing setOutreachDispo + V1 substrate already gate by actor authentication; the new dispatchers respect assignee_id boundaries via the prefs + token lookups. |
| T-04-10-03 | Repudiation | which user triggered the dispatch? | accept | Existing audit captures user.id at server-action entry; reportError extras include taskId on failures. |
| T-04-10-04 | Denial of Service | one dispatcher hangs blocking others | mitigate | Promise.allSettled (NOT Promise.all). One slow Slack/Google API doesn't starve the other. |
| T-04-10-05 | Spoofing | self-assign bypass of integration | accept | If actor self-assigns, none of the three dispatchers fire — same as existing in-app bell behavior; user is acting on their own task. |
</threat_model>

<verification>
1. `npm run typecheck` exits 0
2. Integration test for dispo-actions passes (mocked dispatchers receive calls)
3. Manual smoke per checkpoint passes all 10 steps
4. CI green (typecheck + unit + RTL + Playwright golden paths)
</verification>

<success_criteria>
- setOutreachDispo fans out to all three dispatchers via after() Promise.allSettled
- Self-assign continues to suppress dispatch (parity with V1 in-app bell)
- Manual smoke verifies SLACK-01 + CAL-01 + INTEG-01 end-to-end against prod
- Existing in-app dashboard panel + bell notification continue to work (CONTEXT.md criterion #4)
- CI green (CONTEXT.md criterion #5)
</success_criteria>

<output>
After completion, create `.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-10-SUMMARY.md` documenting:
- The exact diff to setOutreachDispo (3-5 lines + 3 imports)
- Which path was taken for due_at update (A/B/C from Task 2 behavior); if B, document the deferral
- The manual smoke results (which steps passed; any failure modes encountered + resolutions)
- A pointer to the next milestone or phase: "v2.1 milestone is now feature-complete. Run /gsd-verify-work 04 to mint VERIFICATION.md."
</output>
