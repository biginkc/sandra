---
phase: 04-tasks-integrations-v2-slack-google-calendar
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - src/lib/supabase/types.ts
autonomous: false
requirements: [INTEG-01, SLACK-01, CAL-01]

must_haves:
  truths:
    - "Migrations 052 + 053 are applied to BOTH prod and test Supabase projects via db-migrate.yml"
    - "Schema cache reflects user_oauth_tokens, user_integration_prefs, and the three new tasks columns"
    - "src/lib/supabase/types.ts is regenerated to match the live schema"
    - "Integration tests from Plan 02/03 now pass against the test DB"
  artifacts:
    - path: "src/lib/supabase/types.ts"
      provides: "TypeScript types for user_oauth_tokens + user_integration_prefs + tasks (with new columns)"
      contains: "user_oauth_tokens"
  key_links:
    - from: ".github/workflows/db-migrate.yml"
      to: "supabase/migrations/052_user_oauth_tokens.sql + 053_user_integration_prefs.sql"
      via: "supabase db push on push to main"
      pattern: "db-migrate"
---

<objective>
[BLOCKING] Schema-push checkpoint. Verify CI applied migrations 052 + 053 to BOTH prod and test Supabase projects via .github/workflows/db-migrate.yml, then regenerate src/lib/supabase/types.ts so downstream plans (05–10) compile against the live schema.

Why blocking: Sandra's pattern is "write migration .sql files; commit them; CI auto-applies on merge-to-main". Build and type checks pass without the push (types come from the generated file, not the live DB), creating a false-positive verification state. This plan forces the human-in-the-loop confirmation that the migrations actually landed on both projects before tests in Plans 05–10 try to read those tables.

Output: regenerated `src/lib/supabase/types.ts` reflecting the new tables/columns; confirmed green run in db-migrate.yml.
</objective>

<execution_context>
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-CONTEXT.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-RESEARCH.md
@.github/workflows/db-migrate.yml
@supabase/migrations/052_user_oauth_tokens.sql
@supabase/migrations/053_user_integration_prefs.sql
@src/lib/supabase/types.ts
@AGENTS.md

<gotchas>
- Sandra migrations are CI-only. NEVER run `supabase db push` against prod manually. NEVER use the Supabase MCP `apply_migration` tool against prod (per project memory: "Sandra migrations: CI only").
- `db-migrate.yml` runs on push to `main` AND on `paths: ['supabase/migrations/**']`. If migrations 052/053 have not landed on main yet, this checkpoint blocks until they do.
- `supabase/migrations/051_tasks_table.sql` was the most recent applied migration as of phase planning. After this plan, prod and test schema_migrations should both list 052 and 053.
</gotchas>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm CI applied migrations 052 and 053 to prod and test</name>
  <what-built>
    Plans 02 and 03 created `supabase/migrations/052_user_oauth_tokens.sql` and `supabase/migrations/053_user_integration_prefs.sql`. Sandra's `.github/workflows/db-migrate.yml` auto-applies these to both prod (project copflsklaefwzipsrjqz) and test (project ncsngxlcyxylaeskiteu) on push to `main`.
  </what-built>
  <how-to-verify>
    Before this checkpoint can pass:

    1. The branch containing migrations 052 and 053 must be merged to `main`. If not yet:
       - Open the PR with the Plan 02 + 03 work
       - Merge to main
       - Wait for `Apply Supabase migrations to prod and test` workflow run to start

    2. Check the workflow run was green for BOTH jobs:
       ```
       gh run list --workflow="Apply Supabase migrations to prod and test" --limit 3
       gh run view &lt;run-id&gt; --log
       ```
       Both `migrate-prod` and `migrate-test` jobs must complete successfully.

    3. Verify migration is recorded on prod schema_migrations table. Use the Supabase MCP to query the prod project (READ-only — never `apply_migration`):
       - Run `mcp__claude_ai_supabase__execute_sql` against prod with: `select version from schema_migrations where version in ('052', '053') order by version;`
       - Both rows must appear.

    4. Verify migration is recorded on test:
       - Same query against the test project.
       - Both rows must appear.

    5. Verify table existence on both projects:
       - `select to_regclass('public.user_oauth_tokens');` returns `user_oauth_tokens` (not null) on both.
       - `select to_regclass('public.user_integration_prefs');` returns `user_integration_prefs` on both.
       - `select column_name from information_schema.columns where table_name='tasks' and column_name in ('google_calendar_event_id','slack_channel_id','slack_message_ts');` returns 3 rows on both.

    6. Verify SECURITY DEFINER functions exist on both:
       - `select proname from pg_proc where proname in ('get_oauth_token','upsert_oauth_token','delete_oauth_tokens') order by proname;` returns 3 rows on both.

    7. Reply "approved" if all the above check out, OR describe the failure (which job failed; what error; whether to roll forward with a fix-up migration or roll back).
  </how-to-verify>
  <action>This is a human verification checkpoint. The user (Jarrad) follows the steps in &lt;how-to-verify&gt; and replies with the resume-signal. No automated action runs in this task — see Task 2 for the automated regen step that follows.</action>
  <verify>User reply matches the resume-signal contract.</verify>
  <done>User has typed "approved" confirming both prod and test schema_migrations include 052 + 053, both tables exist, and all three SECURITY DEFINER functions are present.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate src/lib/supabase/types.ts and run integration tests</name>
  <files>src/lib/supabase/types.ts</files>
  <read_first>
    - src/lib/supabase/types.ts (existing file — confirm shape; new blocks for user_oauth_tokens and user_integration_prefs will be appended; tasks block will be regenerated to include the three new columns)
    - .planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-PATTERNS.md (lines 686-710 — illustrative shape)
    - package.json (the script that regenerates types — typically `npm run types:generate` or similar; if no script exists, fall back to `supabase gen types typescript --project-id &lt;project-id&gt; --schema public &gt; src/lib/supabase/types.ts`)
  </read_first>
  <action>
    1. Locate the existing types-generation invocation. Check `package.json` scripts for `types:generate`, `db:types`, or `gen:types`. If found, run it.
    2. If no script exists, run:
       ```bash
       npx supabase gen types typescript --project-id copflsklaefwzipsrjqz --schema public > src/lib/supabase/types.ts
       ```
       (project-id from .github/workflows/db-migrate.yml — the `migrate-prod` job's `--project-id` arg)
    3. Verify the regenerated file includes:
       - `user_oauth_tokens` table block with the columns: user_id, provider, token_type, access_token_encrypted, refresh_token_encrypted, access_token_expires_at, scopes, external_account_id, created_at, updated_at
       - `user_integration_prefs` table block with: user_id, channel, enabled, timezone, updated_at
       - `tasks` table block now includes: google_calendar_event_id, slack_channel_id, slack_message_ts (alongside the prior columns)
       - Function definitions for `get_oauth_token`, `upsert_oauth_token`, `delete_oauth_tokens` under the Functions section
    4. Run `npm run typecheck` — must pass.
    5. Run the integration tests deferred from Plan 02:
       ```bash
       npm run test:integration -- src/lib/integrations/tokens/store.integration.test.ts
       ```
       All three behaviors (round-trip, COALESCE, RLS) must pass.

    Do NOT hand-edit types.ts. If regen produces a diff that breaks unrelated callers, that's a signal that someone hand-edited prior to this — open it as a separate Plan to investigate.
  </action>
  <verify>
    <automated>grep -q 'user_oauth_tokens' src/lib/supabase/types.ts &amp;&amp; grep -q 'user_integration_prefs' src/lib/supabase/types.ts &amp;&amp; grep -q 'google_calendar_event_id' src/lib/supabase/types.ts &amp;&amp; grep -q 'slack_channel_id' src/lib/supabase/types.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'user_oauth_tokens' src/lib/supabase/types.ts` returns at least 1
    - `grep -c 'user_integration_prefs' src/lib/supabase/types.ts` returns at least 1
    - `grep -c 'google_calendar_event_id' src/lib/supabase/types.ts` returns at least 1
    - `grep -c 'slack_channel_id' src/lib/supabase/types.ts` returns at least 1
    - `grep -c 'slack_message_ts' src/lib/supabase/types.ts` returns at least 1
    - `grep -c 'get_oauth_token' src/lib/supabase/types.ts` returns at least 1
    - `grep -c 'upsert_oauth_token' src/lib/supabase/types.ts` returns at least 1
    - `npm run typecheck` exits 0
    - `npm run test:integration -- src/lib/integrations/tokens/store.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>Types regenerated; typecheck green; integration tests against the live test DB pass.</done>
</task>

</tasks>

<verification>
1. Migrations 052 + 053 are on both prod and test (verified by checkpoint)
2. types.ts regen includes the new tables and columns
3. typecheck still green
4. Integration tests for token round-trip + RLS + COALESCE all pass against test DB
</verification>

<success_criteria>
- CI run for db-migrate.yml is green for both jobs
- Live prod + test schemas have user_oauth_tokens, user_integration_prefs, and the new tasks columns
- src/lib/supabase/types.ts reflects the live schema
- INTEG-01 acceptance gates partially clear (encryption round-trip + RLS proven against real DB)
</success_criteria>

<output>
After completion, create `.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-04-SUMMARY.md` documenting:
- The db-migrate.yml run id (gh run view link)
- Confirmation that schema_migrations on prod + test both list 052 and 053
- The types.ts diff summary (which Database['public']['Tables'] entries were added)
</output>
