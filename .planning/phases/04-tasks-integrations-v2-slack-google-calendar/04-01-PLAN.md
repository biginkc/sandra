---
phase: 04-tasks-integrations-v2-slack-google-calendar
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/integrations/tokens/oauth-secret.test.ts
  - src/lib/integrations/tokens/store.test.ts
  - src/lib/integrations/tokens/store.integration.test.ts
  - src/lib/integrations/slack/signature.test.ts
  - src/lib/integrations/slack/blocks.test.ts
  - src/lib/integrations/slack/dispatch.test.ts
  - src/lib/integrations/google/dispatch.test.ts
  - src/app/api/oauth/slack/callback/route.test.ts
  - src/app/api/oauth/google/callback/route.test.ts
  - src/app/api/webhooks/slack/actions/route.test.ts
  - src/app/api/webhooks/slack/actions/route.integration.test.ts
  - src/app/(dashboard)/settings/integrations/form.test.tsx
autonomous: true
requirements: [SLACK-01, CAL-01, INTEG-01]
user_setup:
  - service: slack
    why: "Per-user OAuth for outbound DMs + Mark Done button (D-01, D-02, D-03)"
    env_vars:
      - name: SLACK_CLIENT_ID
        source: "Slack App settings → Basic Information → App Credentials"
      - name: SLACK_CLIENT_SECRET
        source: "Slack App settings → Basic Information → App Credentials"
      - name: SLACK_SIGNING_SECRET
        source: "Slack App settings → Basic Information → App Credentials"
    dashboard_config:
      - task: "Create Slack app at api.slack.com/apps"
        location: "https://api.slack.com/apps"
      - task: "Add bot scopes: chat:write, im:write, users:read, users:read.email"
        location: "App settings → OAuth & Permissions → Scopes"
      - task: "Add redirect URL: https://sandra-sooty.vercel.app/api/oauth/slack/callback"
        location: "App settings → OAuth & Permissions → Redirect URLs"
      - task: "Enable Interactivity & Shortcuts; Request URL: https://sandra-sooty.vercel.app/api/webhooks/slack/actions"
        location: "App settings → Interactivity & Shortcuts"
  - service: google
    why: "Per-user OAuth for Calendar event create/update (D-04, D-05)"
    env_vars:
      - name: GOOGLE_OAUTH_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs"
      - name: GOOGLE_OAUTH_CLIENT_SECRET
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs"
      - name: GOOGLE_OAUTH_REDIRECT_URI
        source: "Same Vercel env: https://sandra-sooty.vercel.app/api/oauth/google/callback"
    dashboard_config:
      - task: "Enable Google Calendar API in the Cloud project"
        location: "https://console.cloud.google.com/apis/library/calendar-json.googleapis.com"
      - task: "Create OAuth 2.0 Client ID (Web application)"
        location: "Google Cloud Console → APIs & Services → Credentials"
      - task: "Add authorized redirect URI: https://sandra-sooty.vercel.app/api/oauth/google/callback"
        location: "OAuth client settings"
  - service: oauth_encryption_key
    why: "Symmetric key for pgcrypto pgp_sym_encrypt of stored OAuth tokens (D-09)"
    env_vars:
      - name: OAUTH_TOKEN_ENCRYPTION_KEY
        source: "Generate with `openssl rand -hex 32` — paste into Vercel prod + test env vars; same trust boundary as ANTHROPIC_API_KEY"

must_haves:
  truths:
    - "@slack/web-api and googleapis are pinned in package.json"
    - "Every test file referenced by later plans exists with at least one failing/skipped placeholder test"
    - "OAUTH_TOKEN_ENCRYPTION_KEY is documented in .env.example"
    - "All new test files follow the existing vitest convention so `npm test` discovers them"
  artifacts:
    - path: "package.json"
      provides: "@slack/web-api ^7.15.2 + googleapis ^171.4.0 in dependencies"
      contains: "@slack/web-api"
    - path: "src/lib/integrations/tokens/oauth-secret.test.ts"
      provides: "Test scaffold for OAuthSecret redaction"
    - path: "src/lib/integrations/slack/signature.test.ts"
      provides: "Test scaffold for HMAC verifier"
    - path: ".env.example"
      provides: "Documents the new env vars for local dev"
      contains: "OAUTH_TOKEN_ENCRYPTION_KEY"
  key_links:
    - from: "package.json"
      to: "node_modules/@slack/web-api + node_modules/googleapis"
      via: "npm install"
      pattern: "@slack/web-api.*7\\.15"
---

<objective>
Wave 0 scaffolding for Phase 04. Install the two new SDKs (@slack/web-api, googleapis), create empty (.skip) test placeholders for every file later plans will write, and document the new env vars in .env.example. Nothing in this plan ships behavior — its only job is to make later waves runnable in parallel without missing-dependency or missing-test-file friction.

Purpose: enforce TDD-lite by ensuring test files exist before the source files they cover. Subsequent waves un-skip tests as they implement.
Output: package.json + package-lock.json delta, ~11 test scaffold files, .env.example update.
</objective>

<execution_context>
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-CONTEXT.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-RESEARCH.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-PATTERNS.md
@AGENTS.md
@CLAUDE.md
@package.json
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install SDK dependencies and document env vars</name>
  <files>package.json, package-lock.json, .env.example</files>
  <read_first>
    - package.json (current dependency list — confirm @slack/web-api + googleapis are NOT present yet)
    - .env.example (existing env-var documentation pattern)
    - .planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-RESEARCH.md (Standard Stack section — pinned versions)
    - .planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-CONTEXT.md (D-16 Slack adapter, D-17 googleapis)
  </read_first>
  <action>
    1. Install runtime deps: `npm install @slack/web-api@^7.15.2 googleapis@^171.4.0`
    2. Verify they appear under "dependencies" (not devDependencies) in package.json — they're used by route handlers.
    3. Add to .env.example (place near existing ANTHROPIC_API_KEY block, alphabetical-ish where reasonable):
       ```
       # Slack OAuth (Phase 04 — task assignment DMs)
       SLACK_CLIENT_ID=
       SLACK_CLIENT_SECRET=
       SLACK_SIGNING_SECRET=

       # Google Calendar OAuth (Phase 04 — task event sync)
       GOOGLE_OAUTH_CLIENT_ID=
       GOOGLE_OAUTH_CLIENT_SECRET=
       GOOGLE_OAUTH_REDIRECT_URI=

       # OAuth token encryption (Phase 04 — pgcrypto pgp_sym_encrypt key)
       # Generate locally with: openssl rand -hex 32
       OAUTH_TOKEN_ENCRYPTION_KEY=
       ```
    4. Do NOT add real values anywhere — these get pasted into Vercel by Jarrad outside this plan.
    5. Run `npm run typecheck` to confirm the new deps don't break the build.
  </action>
  <verify>
    <automated>grep -q '"@slack/web-api"' package.json &amp;&amp; grep -q '"googleapis"' package.json &amp;&amp; grep -q 'OAUTH_TOKEN_ENCRYPTION_KEY' .env.example &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@slack/web-api"' package.json` returns 1
    - `grep -c '"googleapis"' package.json` returns 1
    - `node -e "console.log(require('./package.json').dependencies['@slack/web-api'])"` prints a version starting with `^7.`
    - `node -e "console.log(require('./package.json').dependencies['googleapis'])"` prints a version starting with `^171.`
    - `grep -c 'SLACK_CLIENT_ID' .env.example` returns 1
    - `grep -c 'GOOGLE_OAUTH_CLIENT_ID' .env.example` returns 1
    - `grep -c 'OAUTH_TOKEN_ENCRYPTION_KEY' .env.example` returns 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Both SDKs installed and pinned; env vars documented; typecheck still green.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold all test files with placeholder skipped tests</name>
  <files>
    src/lib/integrations/tokens/oauth-secret.test.ts,
    src/lib/integrations/tokens/store.test.ts,
    src/lib/integrations/tokens/store.integration.test.ts,
    src/lib/integrations/slack/signature.test.ts,
    src/lib/integrations/slack/blocks.test.ts,
    src/lib/integrations/slack/dispatch.test.ts,
    src/lib/integrations/google/dispatch.test.ts,
    src/app/api/oauth/slack/callback/route.test.ts,
    src/app/api/oauth/google/callback/route.test.ts,
    src/app/api/webhooks/slack/actions/route.test.ts,
    src/app/api/webhooks/slack/actions/route.integration.test.ts,
    src/app/(dashboard)/settings/integrations/form.test.tsx
  </files>
  <read_first>
    - .planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Wave 0 Gaps list)
    - src/lib/notifications/format.test.ts (existing vitest unit-test convention — import shape, describe/it structure)
    - src/lib/notifications/dispatch.integration.test.ts (existing integration-test convention — connection setup, beforeAll, env-gate via skipIfNoIntegrationDb)
    - vitest.config.ts (file-pattern rules)
    - vitest.integration.config.ts (separate config for *.integration.test.ts)
    - vitest.rtl.config.ts (RTL config for *.test.tsx)
  </read_first>
  <action>
    For each file in &lt;files&gt;, create a vitest scaffold containing:
    - The import `import { describe, it, expect } from "vitest";`
    - A single `describe("&lt;file basename&gt;", () =&gt; { ... })` block
    - One `it.todo("&lt;short description of the behavior listed in 04-RESEARCH.md Phase Requirements → Test Map for this file&gt;")` placeholder per behavior listed for that file. Use `it.todo`, NOT `it.skip`, so the test runner reports them as pending rather than passing.

    Specific to-do lists per file (from 04-RESEARCH.md → Phase Requirements → Test Map):

    - `src/lib/integrations/tokens/oauth-secret.test.ts`:
      * `it.todo("OAuthSecret.toJSON returns '[REDACTED]'");`
      * `it.todo("OAuthSecret.toString returns '[REDACTED]'");`
      * `it.todo("JSON.stringify({ token }) does not leak the value");`
      * `it.todo("reveal() returns the underlying value");`

    - `src/lib/integrations/tokens/store.test.ts`:
      * `it.todo("getDecryptedToken returns null when no row exists");`
      * `it.todo("getDecryptedToken throws ConfigurationError when OAUTH_TOKEN_ENCRYPTION_KEY is unset");`
      * `it.todo("getDecryptedToken wraps access_token and refresh_token as OAuthSecret");`
      * `it.todo("upsertOAuthToken passes plaintext to RPC; never logs it");`

    - `src/lib/integrations/tokens/store.integration.test.ts`:
      * `it.todo("pgp_sym_encrypt → pgp_sym_decrypt round-trip preserves token bytes");`
      * `it.todo("RLS: user can SELECT own row; cannot read another user's encrypted columns");`
      * `it.todo("upsert with null refresh_token preserves existing refresh_token (COALESCE)");`

    - `src/lib/integrations/slack/signature.test.ts`:
      * `it.todo("verifySlackSignature accepts a valid HMAC over v0:{ts}:{body}");`
      * `it.todo("verifySlackSignature rejects when timestamp is missing or non-numeric");`
      * `it.todo("verifySlackSignature rejects when timestamp is older than 5 minutes");`
      * `it.todo("verifySlackSignature rejects when signature is tampered");`
      * `it.todo("verifySlackSignature uses timingSafeEqual to compare");`

    - `src/lib/integrations/slack/blocks.test.ts`:
      * `it.todo("buildTaskAssignedBlocks returns header + context + actions blocks");`
      * `it.todo("button action_id === 'mark_done' and value === taskId");`
      * `it.todo("snapshot matches a known-good fixture for follow-up + due tomorrow");`

    - `src/lib/integrations/slack/dispatch.test.ts`:
      * `it.todo("dispatchTaskAssignedSlack short-circuits when user pref disabled");`
      * `it.todo("dispatchTaskAssignedSlack short-circuits when no bot token row");`
      * `it.todo("dispatchTaskAssignedSlack calls conversations.open then chat.postMessage");`
      * `it.todo("dispatchTaskAssignedSlack never throws (catches and reportErrors)");`
      * `it.todo("dispatchTaskAssignedSlack persists slack channel + message_ts on the task for later chat.update");`

    - `src/lib/integrations/google/dispatch.test.ts`:
      * `it.todo("dispatchTaskCalendarEvent calls calendar.events.insert with calendarId 'primary'");`
      * `it.todo("dispatchTaskCalendarEvent passes user timezone to start.timeZone and end.timeZone");`
      * `it.todo("dispatchTaskCalendarEvent uses 30-min duration");`
      * `it.todo("oauth2Client.on('tokens', ...) UPSERTs rotated tokens with COALESCE-on-refresh");`
      * `it.todo("dispatchTaskCalendarEventUpdate calls events.update with stored eventId on due_at change");`
      * `it.todo("task completion is a no-op (D-07) — no events.delete call");`

    - `src/app/api/oauth/slack/callback/route.test.ts`:
      * `it.todo("returns 401-equivalent redirect on tampered state");`
      * `it.todo("redirects to /login when not authenticated");`
      * `it.todo("on success calls exchangeSlackCode and persistOauthToken twice when user scopes returned");`

    - `src/app/api/oauth/google/callback/route.test.ts`:
      * `it.todo("returns redirect to /settings/integrations?error=state on bad state");`
      * `it.todo("on success calls exchangeGoogleCode and persistOauthToken once with tokenType='user'");`

    - `src/app/api/webhooks/slack/actions/route.test.ts`:
      * `it.todo("rejects with 401 when signature is invalid");`
      * `it.todo("acks 200 within 3s on valid block_actions payload");`
      * `it.todo("schedules after() to call completeTaskFromSlack and refreshSlackMessage");`
      * `it.todo("validates action_id allowlist (mark_done only)");`
      * `it.todo("validates payload.actions[0].value is a UUID before any DB lookup");`

    - `src/app/api/webhooks/slack/actions/route.integration.test.ts`:
      * `it.todo("Mark Done updates tasks.status to 'completed' for the right task");`
      * `it.todo("does not allow completing a task assigned to a different user");`

    - `src/app/(dashboard)/settings/integrations/form.test.tsx`:
      * `it.todo("renders Connect Slack button when no slack tokens row exists");`
      * `it.todo("renders Disconnect button when slack row present");`
      * `it.todo("toggling Slack pref calls setChannelEnabled action");`
      * `it.todo("renders Connect Google + timezone dropdown when google token absent");`

    Place each file in the path listed in &lt;files&gt;. Create parent directories as needed (e.g., mkdir -p src/lib/integrations/tokens, src/lib/integrations/slack, src/lib/integrations/google, src/app/api/oauth/slack/callback, src/app/api/oauth/google/callback, src/app/api/webhooks/slack/actions, src/app/(dashboard)/settings/integrations).

    Run `npm test -- --run` after — all `.todo` cases should report as pending, no failures. The integration-suite scaffolds are picked up only by `vitest.integration.config.ts`, so they should NOT execute during `npm test`.
  </action>
  <verify>
    <automated>npm test -- --run 2>&amp;1 | grep -E "(failed|passed|todo)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `find src/lib/integrations src/app/api/oauth src/app/api/webhooks/slack 'src/app/(dashboard)/settings/integrations' -type f -name '*.test.*' | wc -l` returns at least 12
    - `grep -lr 'it.todo' src/lib/integrations | wc -l` returns at least 7
    - `grep -lr 'it.todo' 'src/app/(dashboard)/settings/integrations' src/app/api/oauth src/app/api/webhooks | wc -l` returns at least 5
    - `npm test -- --run 2>&amp;1 | grep -v '^#' | grep -c 'failed' | tr -d ' '` returns 0 (no failed tests — only todos)
    - Each test file imports from "vitest"; `grep -L 'from "vitest"' $(find src/lib/integrations src/app/api/oauth src/app/api/webhooks/slack 'src/app/(dashboard)/settings/integrations' -name '*.test.*')` returns nothing
  </acceptance_criteria>
  <done>Test scaffold tree exists, vitest discovers files cleanly, no test failures, every todo placeholder maps to a behavior listed in 04-RESEARCH.md Phase Requirements → Test Map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → npm registry | `npm install` fetches @slack/web-api + googleapis; pin minor versions to avoid surprise upgrades |
| repo → Vercel env | New env vars (SLACK_*, GOOGLE_OAUTH_*, OAUTH_TOKEN_ENCRYPTION_KEY) cross this boundary; this plan only documents them — values land in Vercel later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | npm dependency | mitigate | Pin to caret major (`^7.15.2`, `^171.4.0`); commit package-lock.json so a fresh install resolves to the audited tree. |
| T-04-01-02 | Information Disclosure | .env.example | accept | Only contains keys, no values. Documented for dev onboarding. |
| T-04-01-03 | Spoofing | placeholder tests | accept | Tests use `it.todo` so a forgotten implementation later shows up in CI as a pending test, not a silently-passing one. |
</threat_model>

<verification>
1. `npm run typecheck` exits 0
2. `npm test -- --run` exits 0 with no failures (todos report as pending)
3. Both SDKs are listed in package.json under "dependencies"
4. .env.example documents the three new env-var groups
5. Every test file in 04-RESEARCH.md → Wave 0 Gaps exists at the listed path
</verification>

<success_criteria>
- @slack/web-api and googleapis are installed at the versions specified in 04-RESEARCH.md → Standard Stack
- All 12 test files exist with `it.todo` placeholders mapped to behaviors in 04-RESEARCH.md → Phase Requirements → Test Map
- `.env.example` documents SLACK_*, GOOGLE_OAUTH_*, OAUTH_TOKEN_ENCRYPTION_KEY
- `npm run typecheck` and `npm test -- --run` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-01-SUMMARY.md` documenting:
- Exact pinned versions installed
- Path list of test scaffolds created
- Confirmation that env-var keys are in .env.example (no values)
</output>
