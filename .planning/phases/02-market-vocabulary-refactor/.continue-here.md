---
context: phase
phase: 02-market-vocabulary-refactor
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-05-05T19:48:04Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Backfill migration is 046, NOT 045 — the plan already reflects this but the commit history references "migration 045" in older messages. The actual file is `046_backfill_property_county_id_from_fips.sql`. The integration test must be named `046_backfill_property_county_id_from_fips.integration.test.ts`.
- [ ] CONSTRAINT: fips_codes table is now seeded (via migration 047) — the counties table has fips_code populated for all 21 rows. Do not assume it's empty.
- [ ] CONSTRAINT: 1,096 properties have county_id set on prod; 1,437 have county_id=NULL + market='Kansas City' (intentional no-ops per D-05). Expect these counts in the integration test — adjust if the test DB differs.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming CI success = data success | The PR #105 CI run for migrate-prod showed ✓ but 043/044 weren't applied because `--include-all` was missing. Always verify schema_migrations AND data counts after a CI migration run. | advisory | After any migration CI run, query schema_migrations AND spot-check a data count before declaring success. |
| fips_codes reference table empty | fips_codes was created in 001_initial.sql but never seeded. Any future migration that subqueries fips_codes must seed it first OR verify it's populated. | advisory | Before writing a migration that JOINs fips_codes, run `SELECT COUNT(*) FROM fips_codes` against the target DB. |

<current_state>
On branch `main`. All 4 Wave plans (02-01 through 02-04) are done. The migration chain is complete:
- 043: counties.fips_code column + csv_imports.county_id FK ✓
- 044: 21 counties seeded ✓
- 045: outreach_dispo feature ✓
- 046: backfill properties.county_id (renumbered from 045) ✓
- 047: fips_codes seed + counties.fips_code fix + backfill re-run ✓

Prod: 1,096 properties backfilled, 1,437 legacy KS intentional no-ops. Test DB: same migrations applied.
649 tests green (524 unit + 125 RTL).

Waiting to execute plan 02-05 (Wave 4) — the verification wave.
</current_state>

<completed_work>

This session (schema repair):
- Deleted stray migration `20260505001908` from test DB `supabase_migrations.schema_migrations` via MCP
- Merged local main with `origin/main` (brought in PR #105 outreach_dispo merge)
- Created `046_backfill_property_county_id_from_fips.sql` (renamed from 045) — commit d404408
- Fixed `db-migrate.yml` with `--include-all` flag — commit 5630f4b
- Created `047_seed_fips_codes_and_fix_county_backfill.sql` (seeds fips_codes for 21 counties + re-runs backfill) — commit bb179d4
- Updated STATE.md + 02-05-PLAN.md (045→046 reference) — commit defbcd2
- Verified: prod schema_migrations has 043-047; 1,096 properties now have county_id set

Previously shipped (Waves 1-3 + PR #105):
- 02-01: migration 043 (schema prereq)
- 02-02: migration 044 (county seed) + checkpoint approved
- 02-03: KNOWN_MARKETS/WizardMarket eliminated, csv_imports.county_id thread-through
- 02-04: migration 046 backfill (initially 0 rows; fixed by 047)
- PR #105: outreach_dispo feature (migration 045)
</completed_work>

<remaining_work>

Plan 02-05 — Wave 4 (3 tasks):

**Task 1 (auto, TDD):** Update test fixtures + add migration 046 integration test
- `src/app/(dashboard)/leads/filter.test.ts` — swap 2 fixtures to county-shaped strings, keep 1 "Kansas City" (D-05 coexistence)
- `src/app/(dashboard)/lists/lists.integration.test.ts` — LEAVE "Kansas City" inserts alone (PATTERNS option b)
- `src/app/(dashboard)/leads/tags.integration.test.ts` — LEAVE "Kansas City" inserts alone (PATTERNS option b)
- NEW: `supabase/migrations/046_backfill_property_county_id_from_fips.integration.test.ts` — 4 behaviors: happy path (FIPS JOIN), leave-alone (no signals), CASS jsonb fallback, idempotency

**Task 2 (auto):** Add 2 Playwright specs
- NEW: `tests/playwright/import-wizard-counties.spec.ts` — wizard market dropdown lists county-shaped strings; negative: legacy strings gone
- NEW: `tests/playwright/properties-market-filter.spec.ts` — /properties Market filter pill; `data-testid="filter-market-{name}"` shape

**Task 3 (human checkpoint — BLOCKING):**
- Confirm 043-047 in schema_migrations on prod + test via MCP
- Verify backfill counts (~1,096 backfilled, ~1,437 legacy KS)
- Open /import + /properties in browser → confirm county-shaped vocabulary in dropdowns
- Jarrad replies "approved" to close Phase 02
</remaining_work>

<decisions_made>

- Backfill migration renamed 046 (045 taken by outreach_dispo)
- Migration 047 is the canonical fix for the fips_codes empty-table root cause
- db-migrate.yml needs `--include-all` for out-of-order migration scenarios
- 1,437 "Kansas City" properties intentionally left alone (D-05 + D-09)
- Integration test targets migration 046 (not 047 — 046 has the backfill logic we're testing)
</decisions_made>

<blockers>
None currently — all infrastructure is green. The only "blocker" is the human checkpoint at Task 3 (sign-off required to close Phase 02).
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-market-vocabulary-refactor/02-05-PLAN.md` — full task spec for Wave 4; already updated to reference 046
2. `.planning/phases/02-market-vocabulary-refactor/02-PATTERNS.md` — test strategy (option b: leave "Kansas City" in list/tag integration tests)
3. `.planning/phases/02-market-vocabulary-refactor/02-CONTEXT.md` — county list, D-05 decisions, D-09 no-synthetic-row rule

## Infrastructure State
- Branch: `main` (all Phase 02 work + outreach_dispo is on main)
- Prod DB (`copflsklaefwzipsrjqz`): migrations 043-047 applied; 1,096 props backfilled
- Test DB (`ncsngxlcyxylaeskiteu`): migrations 043-047 applied; stray migration removed
- CI: `db-migrate.yml` green on both jobs (last run: defbcd2)
- Tests: 649 passing (524 unit + 125 RTL)
- No running dev server required for Tasks 1+2 (tests use test DB directly); Playwright needs `next dev` running

<context>
Wave 4 is the verification wave — no new schema changes, no new features. It exists to lock the behavior of what Waves 1-3 shipped via tests that can't regress. The two Playwright specs are the key deliverable (per feedback_verify_with_playwright.md: never ask Jarrad to check a page — verify ourselves). The integration test for 046 locks the backfill semantics. The human checkpoint closes Phase 02 officially.

The migration 047 fix was discovered during resume — the root cause was fips_codes being empty since day 1. This is now fully resolved on both DBs.
</context>

<next_action>
Run `/gsd-execute-phase 02` on branch `main`. The executor will pick up plan 02-05 (Wave 4). All 3 tasks are in the plan. The first task is TDD (write test cases first, then code). The third task is a human checkpoint — pause and surface the 7-step verification checklist to Jarrad before closing Phase 02.
</next_action>
