---
phase: 05-prospects-filter-drawer
plan: 03
type: execute
wave: 1
depends_on: [02]
files_modified:
  - src/lib/supabase/types.ts
autonomous: false
requirements: [R6, R7]

must_haves:
  truths:
    - "Migration 055_saved_filters.sql is applied to BOTH prod and test Supabase projects via db-migrate.yml"
    - "schema_migrations table on prod and test both list version 055"
    - "saved_filters table exists on both projects with all 11 columns + 5 base preset rows for BMH org"
    - "src/lib/supabase/types.ts is regenerated to include the saved_filters table"
    - "Plan 02's integration test now passes against the test DB"
  artifacts:
    - path: "src/lib/supabase/types.ts"
      provides: "TypeScript types for saved_filters table"
      contains: "saved_filters"
  key_links:
    - from: ".github/workflows/db-migrate.yml"
      to: "supabase/migrations/055_saved_filters.sql"
      via: "supabase db push on push to main"
      pattern: "db-migrate"
---

<objective>
[BLOCKING] Schema-push checkpoint. Verify CI applied migration 055 (or whichever slot Plan 02 chose) to BOTH prod and test Supabase projects via .github/workflows/db-migrate.yml, then regenerate src/lib/supabase/types.ts so downstream plans (04, 05, ..., 10) compile against the live schema and the multi-user RLS integration test from Plan 02 passes.

Why blocking: Sandra's pattern is "write migration .sql files; commit them; CI auto-applies on merge-to-main". TypeScript type-check passes without the push (types come from the generated file, not the live DB), creating a false-positive verification state. This plan forces the human-in-the-loop confirmation that the migration actually landed on both projects before tests in Plans 04+ try to read saved_filters.

This is identical in structure to `04-04-PLAN.md` — Sandra's canonical schema-push gate.

Output: regenerated `src/lib/supabase/types.ts` reflecting the new `saved_filters` table; confirmed green run in db-migrate.yml; Plan 02 integration test goes from RED to GREEN.
</objective>

<execution_context>
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarradhenry/Sites/Sandra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prospects-filter-drawer/05-CONTEXT.md
@.planning/phases/05-prospects-filter-drawer/05-RESEARCH.md
@.planning/phases/05-prospects-filter-drawer/05-02-SUMMARY.md
@.planning/phases/04-tasks-integrations-v2-slack-google-calendar/04-04-PLAN.md
@.github/workflows/db-migrate.yml
@supabase/migrations/055_saved_filters.sql
@src/lib/supabase/types.ts
@AGENTS.md

<gotchas>
- Sandra migrations are CI-only. NEVER run `supabase db push` against prod manually. NEVER use Supabase MCP `apply_migration` against prod (memory `feedback_migrations_only_via_ci.md`).
- `db-migrate.yml` runs on push to `main` AND on `paths: ['supabase/migrations/**']`. If the migration .sql commit hasn't merged yet, this checkpoint blocks until it does.
- This plan does NOT execute the migration. It WAITS for CI to apply it, then regenerates types.
- If the actual slot Plan 02 chose was 056 (Phase 04 collision), substitute 056 everywhere below.
- Prod project ref: `copflsklaefwzipsrjqz`. Test project ref: `ncsngxlcyxylaeskiteu`. (From `.github/workflows/db-migrate.yml`.)
- There is no `npm run gen:types` script in package.json (verified at planning time). Use the npx supabase command directly.
</gotchas>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm CI applied migration 055 to prod and test</name>
  <what-built>
    Plan 02 created `supabase/migrations/055_saved_filters.sql` (or 056_saved_filters.sql if Phase 04 claimed 055). Sandra's `.github/workflows/db-migrate.yml` auto-applies these to both prod (project copflsklaefwzipsrjqz) and test (project ncsngxlcyxylaeskiteu) on push to `main`.
  </what-built>
  <how-to-verify>
    Per memory `feedback_migrations_only_via_ci.md`, the GitHub Actions log + Supabase dashboard are the PRIMARY evidence sources for migration application. MCP `execute_sql` is a FALLBACK only used to disambiguate when the log is unclear. Never use `apply_migration` against prod.

    Before this checkpoint can pass:

    1. The branch containing migration 055 must be merged to `main`. If not yet:
       - Open the PR with the Plan 02 work
       - Get review and merge to main
       - Wait for `Apply Supabase migrations to prod and test` workflow run to start

    2. PRIMARY evidence — the GitHub Actions run log:
       ```bash
       gh run list --workflow="Apply Supabase migrations to prod and test" --limit 3
       gh run view <run-id> --log | grep -E '055_saved_filters|Applying migration'
       ```
       Confirm BOTH `migrate-prod` and `migrate-test` jobs:
         - completed successfully (green status)
         - logged the application of `055_saved_filters.sql` (one line per project)
       If both jobs are green AND the log mentions 055 for both projects, that is sufficient evidence to proceed to Task 2.

    3. PRIMARY evidence — the Supabase dashboard:
       Open both project URLs to visually confirm the table exists and base presets are seeded:
       - Prod: https://supabase.com/dashboard/project/copflsklaefwzipsrjqz/editor → `public.saved_filters` table → 5 rows where `is_base = true`.
       - Test: https://supabase.com/dashboard/project/ncsngxlcyxylaeskiteu/editor → same.
       The agent runs `open <url>` for each so the user sees them in Chrome (per memory `feedback_open_links_in_browser.md`).

    4. FALLBACK only if step 2 or step 3 is ambiguous (e.g., the log was truncated, the dashboard didn't load, or the migration name in the log doesn't match) — use Supabase MCP read-only queries:
       - `mcp__claude_ai_supabase__execute_sql` against prod (copflsklaefwzipsrjqz): `select version from schema_migrations where version = '055';` — one row expected.
       - Same query against test (ncsngxlcyxylaeskiteu).
       - `select to_regclass('public.saved_filters');` returns `saved_filters` (not null) on both.
       - `select polname from pg_policies where schemaname='public' and tablename='saved_filters' order by polname;` returns 5 rows: `saved_filters_delete_own`, `saved_filters_insert_own`, `saved_filters_read_own_plus_base`, `saved_filters_service_all`, `saved_filters_update_own`.
       - `select count(*) from public.saved_filters where is_base = true and org_id = '00000000-0000-0000-0000-000000000bbb';` returns 5.
       - `select name from public.saved_filters where is_base = true and org_id = '00000000-0000-0000-0000-000000000bbb' order by name;` returns Cold, Engaged, High Equity, Stacked, Vacant.

       NEVER use `mcp__claude_ai_supabase__apply_migration` against prod. The MCP is read-only here.

    5. Reply "approved" if step 2 + step 3 (or step 4 fallback) check out, OR describe the failure (which job failed; what error; whether to roll forward with a fix-up migration or roll back).
  </how-to-verify>
  <action>This is a human verification checkpoint. The user (Jarrad) follows the steps in &lt;how-to-verify&gt; and replies with the resume-signal. No automated action runs in this task — see Task 2 for the automated regen step that follows. The agent should NOT attempt to run `apply_migration` itself; the user is the only gate.</action>
  <verify>User reply matches the resume-signal contract.</verify>
  <acceptance_criteria>
    - User has typed "approved" confirming both prod and test schema_migrations include 055.
    - All 5 RLS policies exist on both projects.
    - Base preset count = 5 on both projects (BMH org).
  </acceptance_criteria>
  <done>User confirmed the migration is applied to both projects with the correct policies and seed data.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate src/lib/supabase/types.ts and run Plan 02 integration test</name>
  <files>src/lib/supabase/types.ts</files>
  <read_first>
    - src/lib/supabase/types.ts (existing file — confirm shape; new block for saved_filters will be appended; types for jsonb columns are typically `Json | null`)
    - .github/workflows/db-migrate.yml (the `migrate-prod` job — extract the project-id `copflsklaefwzipsrjqz` for the gen-types CLI invocation)
    - package.json scripts section (verify there is NO `gen:types` / `db:types` / `types:generate` script — fall back to the npx command)
    - supabase/migrations/055_saved_filters.sql (column shapes that must appear in the generated types)
  </read_first>
  <action>
    1. Locate the existing types-generation invocation. Check `package.json` scripts for `types:generate`, `db:types`, or `gen:types`. As of this plan's writing, none exist.
    2. Run the supabase gen-types command directly:
       ```bash
       npx -y supabase gen types typescript --project-id copflsklaefwzipsrjqz --schema public > src/lib/supabase/types.ts
       ```
       (project-id from .github/workflows/db-migrate.yml — the `migrate-prod` job's `--project-id` arg)
    3. Verify the regenerated file includes:
       - `saved_filters` table block with columns: id, org_id, user_id, name, filters_json, starred, is_base, last_run_at, last_count, created_at, updated_at
       - The `filters_json` column typed as `Json` (Supabase's generic jsonb type alias)
    4. Run `npm run typecheck` — must pass.
    5. Run Plan 02's integration test (now that migration 055 is on test):
       ```bash
       npm run test:integration -- --run supabase/migrations/055_saved_filters.integration.test.ts
       ```
       All 6 behaviors (base seed count, idempotency, RLS A-can't-read-B, both see base, non-member sees zero, A can't update B) must pass.

    Do NOT hand-edit types.ts. If regen produces a diff that breaks unrelated callers, that's a signal that someone hand-edited prior to this — open it as a separate Plan to investigate.

    If `npx supabase gen types` fails because the local supabase CLI cannot auth to the prod project (no SUPABASE_ACCESS_TOKEN env var locally), fall back to:
    ```bash
    npx -y supabase login    # interactive — Jarrad pastes his PAT
    npx -y supabase gen types typescript --project-id copflsklaefwzipsrjqz --schema public > src/lib/supabase/types.ts
    ```
    Per memory `feedback_step_by_step_for_manual_procedures.md`: pause and walk Jarrad through `supabase login` ONE step at a time if the auth step is needed.
  </action>
  <verify>
    <automated>grep -q 'saved_filters' src/lib/supabase/types.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:integration -- --run supabase/migrations/055_saved_filters.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'saved_filters' src/lib/supabase/types.ts` returns at least 1.
    - `grep -c 'filters_json' src/lib/supabase/types.ts` returns at least 1.
    - `grep -c 'is_base' src/lib/supabase/types.ts` returns at least 1.
    - `grep -c 'starred' src/lib/supabase/types.ts` returns at least 1.
    - `grep -c 'last_run_at' src/lib/supabase/types.ts` returns at least 1.
    - `npm run typecheck` exits 0.
    - `npm run test:integration -- --run supabase/migrations/055_saved_filters.integration.test.ts` exits 0 (all 6 tests green).
  </acceptance_criteria>
  <done>Types regenerated; typecheck green; Plan 02's integration test (RED before this plan) is now GREEN.</done>
</task>

</tasks>

<verification>
1. Migration 055 is applied to BOTH prod and test (verified by user-checkpoint via Supabase MCP read-only queries).
2. types.ts regen includes the saved_filters table.
3. typecheck still green.
4. Plan 02 integration test passes against test DB.
</verification>

<success_criteria>
- CI run for db-migrate.yml is green for both jobs (prod + test).
- Live prod + test schemas have saved_filters with 5 RLS policies + 5 base presets.
- src/lib/supabase/types.ts reflects the live schema.
- Phase 05 R7 acceptance gate clears (idempotent seed + RLS isolation proven against the real test DB).
- Plan 02 integration test transitions from RED to GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prospects-filter-drawer/05-03-SUMMARY.md` documenting:
- The db-migrate.yml run id (gh run view link)
- Confirmation that schema_migrations on prod + test both list 055 (or whichever slot was chosen)
- The types.ts diff summary (Database['public']['Tables']['saved_filters'] block — paste the column types so downstream plans can quote them in their `<read_first>` blocks)
</output>
