---
phase: 01.5-sandra-design-system-retrofit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
autonomous: true
requirements:
  - DS-01
  - DS-05

must_haves:
  truths:
    - "package.json declares @sandra/tokens via file: reference"
    - "npm install materializes the symlink under node_modules/@sandra/tokens"
    - "globals.css imports @sandra/tokens/theme.css after line 3"
    - "The local :root { ... } warm-paper token block (lines 70-103) is gone from globals.css"
    - "No competing :root token definitions remain in globals.css"
    - "npm run verify passes (typecheck + jest unit + RTL)"
  artifacts:
    - path: "package.json"
      provides: "Declares @sandra/tokens dependency"
      contains: '"@sandra/tokens": "file:../Sandra Design System"'
    - path: "src/app/globals.css"
      provides: "Token import + clean CSS layer"
      contains: '@import "@sandra/tokens/theme.css";'
  key_links:
    - from: "src/app/globals.css"
      to: "../Sandra Design System/tokens/theme.css"
      via: "@import"
      pattern: '@import "@sandra/tokens/theme\.css"'
---

<objective>
Wire the @sandra/tokens design token package into Sandra CRM.

Purpose: Establish the single source of truth for visual tokens (warm-paper palette, radius scale, Tailwind color utilities) from the sibling repo. After this plan, the `:root` token block no longer lives in globals.css — it comes from the shared package. This is the prerequisite for all Wave 2 plans.

Output:
- package.json with `@sandra/tokens` file: reference
- globals.css importing `@sandra/tokens/theme.css` (after line 3)
- Local `:root { ... }` block removed from globals.css
- `npm install` symlink materialized
</objective>

<execution_context>
@/Users/jarradhenry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarradhenry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-sandra-design-system-retrofit/01.5-CONTEXT.md
</context>

<interfaces>
<!-- Key references for the executor -->

From ../Sandra Design System/package.json:
```json
{
  "name": "@sandra/tokens",
  "exports": {
    "./theme.css": "./tokens/theme.css"
  }
}
```

From src/app/globals.css (current state):
- Line 1: @import "tailwindcss";
- Line 2: @import "tw-animate-css";
- Line 3: @import "shadcn/tailwind.css";
- Line 5: @custom-variant dark (&:is(.dark *));
- Lines 6–54: @theme inline { ... } (color + radius Tailwind mappings)
- Lines 56–68: comment block (warm-paper palette notes)
- Lines 70–103: :root { ... } (warm-paper CSS variables — REMOVE THIS BLOCK)
- Lines 105–137: .dark { ... } (dark mode overrides)
- Lines 139–149: @layer base { ... }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add @sandra/tokens to package.json and install</name>
  <files>package.json</files>
  <read_first>
    - package.json — read current dependencies block to confirm @sandra/tokens is absent before adding
  </read_first>
  <action>
    Edit package.json: inside the "dependencies" object, add the following entry (per D-01):

    ```
    "@sandra/tokens": "file:../Sandra Design System"
    ```

    Place it alphabetically (before "@supabase/ssr"). After editing, run:

    ```bash
    npm install
    ```

    Then verify the symlink was created:

    ```bash
    ls node_modules/@sandra/tokens/tokens/theme.css
    ```

    If the symlink is missing or the file is absent, `npm install` failed — stop and report the error. Do NOT proceed to Task 2 until this symlink exists.
  </action>
  <verify>
    <automated>grep -c '"@sandra/tokens"' package.json && ls node_modules/@sandra/tokens/tokens/theme.css</automated>
  </verify>
  <acceptance_criteria>
    - grep '"@sandra/tokens": "file:../Sandra Design System"' package.json exits 0
    - ls node_modules/@sandra/tokens/tokens/theme.css exits 0 (symlink materialized)
    - npm install produced no errors in its output
  </acceptance_criteria>
  <done>package.json contains the file: reference and node_modules/@sandra/tokens is a valid symlink with theme.css accessible</done>
</task>

<task type="auto">
  <name>Task 2: Update globals.css — add import, remove :root block</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — read the full file to locate the exact :root block boundaries before editing
  </read_first>
  <action>
    Make two edits to src/app/globals.css (per D-02):

    EDIT 1 — Add import after line 3:
    After the line `@import "shadcn/tailwind.css";` (currently line 3), insert:
    ```css
    @import "@sandra/tokens/theme.css";
    ```
    The file should then read:
    ```css
    @import "tailwindcss";
    @import "tw-animate-css";
    @import "shadcn/tailwind.css";
    @import "@sandra/tokens/theme.css";
    ```

    EDIT 2 — Remove the :root block:
    Remove the entire block that starts with `:root {` and ends with its closing `}`. This block currently occupies lines 70–103 and contains the warm-paper CSS variable declarations (--background, --foreground, --card, --card-foreground, etc. through --sidebar-ring). Also remove the comment block immediately above it (lines 56–68, the "Design refresh — warm paper palette" comment). The `.dark { ... }` block and `@layer base { ... }` block are NOT removed.

    After editing, the file should:
    - Have @import "@sandra/tokens/theme.css" on line 4
    - Have NO `:root { --background:` anywhere in the file
    - Retain .dark { ... } and @layer base { ... }
    - Retain @theme inline { ... }
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '@import "@sandra/tokens/theme.css"' src/app/globals.css outputs 1
    - grep -v '^[[:space:]]*/\*' src/app/globals.css | grep -c '^\s*:root\s*{' outputs 0
    - grep -c '--background: #fdfcfb' src/app/globals.css outputs 0
    - npm run verify exits 0 (typecheck + unit + RTL all pass)
  </acceptance_criteria>
  <done>globals.css imports from @sandra/tokens, the local :root token block is gone, and the full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → build | sibling repo files read via npm file: symlink into build pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-01-01 | Tampering | sibling repo symlink | accept | file: references are dev-only; no untrusted party can modify the sibling repo on this machine; prod build reads from node_modules copy |
| T-01.5-01-02 | Denial of Service | npm install failure | accept | plan's verify step catches this immediately; no runtime risk |
</threat_model>

<verification>
After both tasks:
1. `grep '"@sandra/tokens": "file:../Sandra Design System"' package.json` — exits 0
2. `ls node_modules/@sandra/tokens/tokens/theme.css` — exits 0
3. `grep -c '@import "@sandra/tokens/theme.css"' src/app/globals.css` — outputs 1
4. `grep -c ':root' src/app/globals.css` — should show 0 (the :root block is gone; .dark is still there)
5. `npm run verify` — exits 0
</verification>

<success_criteria>
- package.json contains `"@sandra/tokens": "file:../Sandra Design System"` in dependencies
- node_modules/@sandra/tokens symlink exists with tokens/theme.css accessible
- globals.css line 4 is `@import "@sandra/tokens/theme.css";`
- No `:root { --background:` in globals.css
- `npm run verify` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-sandra-design-system-retrofit/01.5-01-SUMMARY.md`
</output>
