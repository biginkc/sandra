---
phase: 01.5-sandra-design-system-retrofit
verified: 2026-05-04T20:10:00Z
status: verified
score: 5/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open the Vercel preview for this phase's branch (or the prod URL https://sandra-sooty.vercel.app) and visually inspect /properties, /lists, /jobs, /templates"
    expected: "All four routes show the rounded-2xl card shell (DataTableShell) with a DataTableFooter count strip. /properties shows circular page-button pagination instead of Prev/Next links. Search pill has pill-shaped rounded-full input with leading magnifying-glass icon on all routes. No layout regressions or token-value shifts (colors, spacing) vs the warm-paper baseline."
    why_human: "Visual regression is explicitly in DS-05's success criteria ('visual verification on every dashboard route shows only intended token-driven color shifts, no layout regressions'). Cannot be verified programmatically without Playwright baseline screenshots — CI Playwright is confirmed green per SUMMARY reports but no screenshot comparison tooling was configured for this phase."
---

# Phase 01.5: Sandra Design System Retrofit — Verification Report

**Phase Goal:** Adopt @sandra/tokens (Layer 1) and the relevant Layer 2 registry components (SearchInputPill, DataTableShell, DataTableFooter, CircularPagination) into Sandra CRM. Visual-overhaul-only phase — no behavior change, no new features.
**Verified:** 2026-05-04T20:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | package.json declares @sandra/tokens via file: reference; npm install materializes the symlink | VERIFIED | `"@sandra/tokens": "file:../Sandra Design System"` confirmed in package.json; symlink was absent at start of verification but re-materialized cleanly after `npm install` — sibling repo at `/Users/jarradhenry/Sites/Sandra Design System/` exists with `tokens/theme.css` present; `node_modules/@sandra/tokens/tokens/theme.css` accessible after install |
| 2 | src/app/globals.css imports @sandra/tokens/theme.css and the local :root { ... } token block is removed | VERIFIED | Line 4 of globals.css is `@import "@sandra/tokens/theme.css";`; `grep -n ':root' globals.css` returns no matches; `grep -n 'fdfcfb' globals.css` returns no matches — warm-paper hex values are gone from the file |
| 3 | TableToolbar.Search renders SearchInputPill (replacing the bare input from Phase 1) — call site signature unchanged | VERIFIED | `grep -c 'SearchInputPill' table-toolbar.tsx` → 2 (import on line 7 + usage on line 96); `import.*Input.*@/components/ui/input` → 0; `import.*Search.*lucide` → 0; X clear button preserved at line 111 (`aria-label="Clear search"`); external props ariaLabel/placeholder/testId/className unchanged |
| 4 | Every CRM table (/properties, /lists, /jobs, /templates) is wrapped in DataTableShell + DataTableFooter; pagination strip on /properties uses CircularPagination | VERIFIED | DataTableShell: 3 refs in prospects-table.tsx, 3 in lists-table.tsx, 3 in jobs-list.tsx, 3 in templates-list.tsx; DataTableFooter: 3 refs in each; CircularPagination: 4 refs in prospects-table.tsx, 0 in the other three (correct per D-08); old `"border-border rounded-md border"` wrapper: 0 occurrences in all four files; CircularPagination is wired to `ts.navigate` + `ts.buildHref` with page URL param |
| 5 | CI green: typecheck + unit + RTL | VERIFIED | `npm run typecheck` exits 0; vitest unit: 41 test files / 506 tests passed; vitest RTL: 16 test files / 106 tests passed; total 612/612 |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `package.json` | Declares @sandra/tokens dependency | VERIFIED | `"@sandra/tokens": "file:../Sandra Design System"` present |
| `node_modules/@sandra/tokens/tokens/theme.css` | Symlink materialized | VERIFIED (after npm install) | Was absent at verification start; re-materialized cleanly after `npm install` — this is expected behavior on a fresh checkout or after worktree operations |
| `src/app/globals.css` | Imports @sandra/tokens/theme.css; no :root block | VERIFIED | Line 4 import confirmed; :root and warm-paper hex values absent |
| `src/components/ui/search-input-pill.tsx` | SearchInputPill component | VERIFIED | Exists; `export function SearchInputPill` confirmed; includes React 19 ref-as-prop enhancement beyond registry source |
| `src/components/ui/data-table-shell.tsx` | DataTableShell + DataTableFooter | VERIFIED | Exists; both named exports confirmed |
| `src/components/ui/circular-pagination.tsx` | CircularPagination component | VERIFIED | Exists; `export function CircularPagination` confirmed |
| `src/components/table/table-toolbar.tsx` | TableToolbarSearch using SearchInputPill | VERIFIED | SearchInputPill imported and used; bare Input + lucide Search removed; X clear button preserved |
| `src/app/(dashboard)/properties/prospects-table.tsx` | DataTableShell + DataTableFooter + CircularPagination; page + totalPages props | VERIFIED | All three components imported and used; `page: number` and `totalPages: number` in Props; CircularPagination wired to ts.navigate/ts.buildHref |
| `src/app/(dashboard)/properties/page.tsx` | Passes page + totalPages; old Prev/Next removed | VERIFIED | `page={page}` and `totalPages={totalPages}` on ProspectsTable call; "← Prev" and "Next →" absent |
| `src/app/(dashboard)/lists/lists-table.tsx` | DataTableShell + DataTableFooter; uses total prop (not rows.length) | VERIFIED | DataTableShell + DataTableFooter present; `total.toLocaleString()` in footer copy; `_total` alias removed |
| `src/app/(dashboard)/jobs/jobs-list.tsx` | DataTableShell + DataTableFooter; no CircularPagination | VERIFIED | Both shell components present; 0 CircularPagination references |
| `src/app/(dashboard)/templates/templates-list.tsx` | DataTableShell + DataTableFooter; no CircularPagination | VERIFIED | Both shell components present; 0 CircularPagination references |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| src/app/globals.css | ../Sandra Design System/tokens/theme.css | @import "@sandra/tokens/theme.css" | WIRED | Line 4 confirmed; sibling repo and theme.css file both exist |
| src/components/table/table-toolbar.tsx | src/components/ui/search-input-pill.tsx | import { SearchInputPill } | WIRED | Import line 7; usage line 96; aria-label forwarded via ...props spread |
| src/app/(dashboard)/properties/prospects-table.tsx | src/components/ui/data-table-shell.tsx | import { DataTableShell, DataTableFooter } | WIRED | Named import confirmed; DataTableShell at lines 707/870; DataTableFooter at line 846 |
| src/app/(dashboard)/properties/prospects-table.tsx | src/components/ui/circular-pagination.tsx | import { CircularPagination } | WIRED | Import line 21; usage at line 853; onPageChange wired to ts.navigate + ts.buildHref |
| src/app/(dashboard)/lists/lists-table.tsx | src/components/ui/data-table-shell.tsx | import { DataTableShell, DataTableFooter } | WIRED | Import line 8; DataTableShell at lines 117/255 |
| src/app/(dashboard)/jobs/jobs-list.tsx | src/components/ui/data-table-shell.tsx | import { DataTableShell, DataTableFooter } | WIRED | Import line 23; DataTableShell at lines 278/446 |
| src/app/(dashboard)/templates/templates-list.tsx | src/components/ui/data-table-shell.tsx | import { DataTableShell, DataTableFooter } | WIRED | Import line 9; DataTableShell at lines 196/323 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| prospects-table.tsx CircularPagination | page, totalPages | Server props from page.tsx (computed from URL param + Math.ceil(total/PAGE_SIZE)) | Yes — server query count drives totalPages | FLOWING |
| prospects-table.tsx DataTableFooter | total, showingFrom, showingTo, page | Server props; showingFrom/To derived from server-passed page | Yes — Supabase query count + URL param | FLOWING |
| lists-table.tsx DataTableFooter | total | Server prop from lists page.tsx | Yes — server-paginated result count | FLOWING |
| jobs-list.tsx DataTableFooter | jobs.length / visibleJobs.length | Realtime subscription state | Yes — Supabase realtime subscription | FLOWING |
| templates-list.tsx DataTableFooter | visible.length / templates.length | useMemo filtered from server-fetched templates prop | Yes — server-fetched array | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — no runnable entry points without starting the Next.js dev server, which is prohibited on this machine per project constraints.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| DS-01 | 01.5-01 | CRM consumes @sandra/tokens; globals.css imports theme.css; no competing :root | SATISFIED | package.json declares file: reference; line 4 import in globals.css; no :root block remaining |
| DS-02 | 01.5-03 | TableToolbar.Search renders SearchInputPill without changing call-site signatures | SATISFIED | SearchInputPill imported and used in table-toolbar.tsx; bare Input + lucide Search removed; external props unchanged |
| DS-03 | 01.5-04, 01.5-05 | Every CRM table wrapped in DataTableShell + DataTableFooter | SATISFIED | All four routes (/properties, /lists, /jobs, /templates) have DataTableShell + DataTableFooter confirmed by grep |
| DS-04 | 01.5-04 | Pagination uses CircularPagination; URL-state behavior preserved | SATISFIED | CircularPagination on /properties wired to ts.navigate/ts.buildHref; page URL param preserved; /lists /jobs /templates correctly omit CircularPagination per D-08 |
| DS-05 | 01.5-01 through 01.5-05 (all) | All Phase 1 tests + existing suite stay green; visual verification shows no layout regressions | PARTIALLY SATISFIED (NEEDS HUMAN) | 612/612 tests pass; visual regression requires human inspection — see Human Verification Required section |

### Anti-Patterns Found

No anti-patterns found. Scanned all eight modified source files for TODO/FIXME/PLACEHOLDER/stub patterns — zero matches.

### Human Verification Required

#### 1. Visual Regression Check — All Dashboard Routes

**Test:** Open the Vercel preview URL for the branch (or `https://sandra-sooty.vercel.app` on main) and navigate through /properties, /lists, /jobs, /templates.

**Expected:**
- All four routes display the rounded-2xl card surround (DataTableShell) with a visible count footer strip (DataTableFooter) below the table
- /properties shows circular page-button pagination buttons instead of the old "← Prev / Next →" link pair
- The search bar on all routes has a pill/rounded-full shape with a leading magnifying-glass icon (SearchInputPill)
- Token-driven color values (background, card, border, sidebar) match the warm-paper palette from the sibling design system — no jarring color differences vs the pre-phase baseline
- No layout spacing regressions on any route — row heights, column widths, and filter panels appear identical to pre-phase except for the card surround and count footer

**Why human:** DS-05 explicitly requires "visual verification on every dashboard route shows only intended token-driven color shifts, no layout regressions." This requires a human eye or a Playwright screenshot baseline comparison. No screenshot baseline was established for this phase, and the dev server cannot be started on this machine.

### Gaps Summary

No automated gaps found. All five success criteria are verified by codebase evidence and the full 612-test suite passes.

One item requires human confirmation before DS-05 can be fully marked satisfied: visual regression check across the four retrofitted dashboard routes (see Human Verification Required above). This is a deliberate DS-05 requirement, not an oversight — the automated suite confirms structural correctness; a human must confirm visual correctness.

**Note on symlink absence at verification start:** `node_modules/@sandra/tokens` was not present when verification began. Running `npm install` re-materialized it in under 3 seconds — the sibling repo exists at the correct relative path and the package.json file: reference is correct. This is a normal state for a worktree or fresh-clone environment and does not indicate a code defect. The package.json declaration (SC-1 requirement) is the durable artifact; the symlink is ephemeral and always re-materializable.

---

_Verified: 2026-05-04T20:10:00Z_
_Verifier: Claude (gsd-verifier)_
