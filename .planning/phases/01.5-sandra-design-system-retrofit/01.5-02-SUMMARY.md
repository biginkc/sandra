---
phase: 01.5-sandra-design-system-retrofit
plan: 02
subsystem: ui

tags: [shadcn, design-system, tailwind, react-19, ref-as-prop]

# Dependency graph
requires:
  - phase: 01.5-sandra-design-system-retrofit
    provides: registry source files in ../Sandra Design System/registry/
provides:
  - SearchInputPill component importable from @/components/ui/search-input-pill
  - DataTableShell + DataTableFooter components importable from @/components/ui/data-table-shell
  - CircularPagination component importable from @/components/ui/circular-pagination
affects: [01.5-wave2 retrofit plans, future table surfaces, lists/jobs/templates pagination]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Registry components copied verbatim into src/components/ui/ (per D-03) — no npx shadcn add"
    - "React 19 ref-as-prop forwarding via React.RefAttributes intersection on props interface"

key-files:
  created:
    - src/components/ui/search-input-pill.tsx
    - src/components/ui/data-table-shell.tsx
    - src/components/ui/circular-pagination.tsx
  modified: []

key-decisions:
  - "Files were already present at HEAD (introduced by an earlier worktree branch); verified contents and acceptance criteria rather than re-overwriting"
  - "Preserved the React.RefAttributes ref-as-prop enhancement on SearchInputPill (added in Wave 2 commit d72daf9 to support TableToolbarSearch ref forwarding) — overwriting from registry would have regressed Wave 2 consumers"

patterns-established:
  - "Verbatim registry copy (no modification, no shadcn CLI) per Phase 01.5 decision D-03"
  - "When registry source must evolve, the consumer-side enhancement is the source of truth and the registry should follow — not the other way around"

requirements-completed: [DS-05]

# Metrics
duration: 3min
completed: 2026-05-04
---

# Phase 01.5 Plan 02: Copy 3 Registry Components Summary

**SearchInputPill, DataTableShell + DataTableFooter, and CircularPagination available under @/components/ui/ for Wave 2 retrofit consumers**

## Performance

- **Duration:** ~3 min (verification only — files already in place)
- **Started:** 2026-05-05T00:27:00Z
- **Completed:** 2026-05-05T00:30:27Z
- **Tasks:** 1
- **Files modified:** 0 (files already present at HEAD; verification confirmed they satisfy acceptance criteria)

## Accomplishments

- Verified all three registry components exist at the expected paths and export the expected named functions
- Confirmed `data-table-shell.tsx` and `circular-pagination.tsx` are byte-identical to the registry source in `../Sandra Design System/registry/`
- Confirmed `search-input-pill.tsx` matches the registry source plus a deliberate ref-as-prop enhancement that consumers depend on (Wave 2 commit `d72daf9`)
- Confirmed `@/lib/utils` `cn` import resolves cleanly in all three files
- `npm run typecheck` passes
- `npm run verify` passes (41 unit test files / 506 tests + 16 RTL test files / 106 tests)

## Task Commits

1. **Task 1: Copy all 3 registry component files into src/components/ui/** — no new commit; the work was already committed in worktree base ancestor `1b886ad feat(01.5-wave1): wire @sandra/tokens + copy 3 registry components`, with a follow-up enhancement to `search-input-pill.tsx` in `d72daf9 feat(01.5-wave2-03): swap TableToolbarSearch inner input → SearchInputPill`. The current plan's acceptance criteria are already satisfied by HEAD.

**Plan metadata:** committed by orchestrator after wave merge.

## Files Created/Modified

- `src/components/ui/search-input-pill.tsx` — pill-shaped search input with leading magnifying glass; props interface intersects `React.RefAttributes<HTMLInputElement>` so consumers can forward refs (required by `TableToolbarSearch`)
- `src/components/ui/data-table-shell.tsx` — wrapper + footer pair providing the standard table surround used across `/properties`, `/lists`, `/jobs`, `/templates`
- `src/components/ui/circular-pagination.tsx` — circular page-button pagination with prev/next chevrons and configurable sibling count

## Decisions Made

- **Did not re-overwrite the existing files from the registry source.** Two are byte-identical; the third (`search-input-pill.tsx`) carries a forward-compatible ref-as-prop enhancement applied in Wave 2 to support `TableToolbarSearch`. Overwriting from registry would have regressed consumer code (Rule 1 — avoid introducing bugs). The plan's acceptance criteria (files exist, named exports present, typecheck passes) are all satisfied without modification.

## Deviations from Plan

### Resolved without code changes

**1. [Rule 1 — Avoid regression] Files already present at HEAD; preserved the Wave 2 ref-forwarding enhancement on SearchInputPill**
- **Found during:** Task 1 — pre-write directory listing
- **Issue:** The plan instructed verbatim copy from the registry source, but all three files were already tracked at HEAD (introduced by ancestor commit `1b886ad`). `search-input-pill.tsx` had been further enhanced in `d72daf9` to forward refs via the React 19 ref-as-prop pattern, which `TableToolbarSearch` and other Wave 2 consumers now depend on. A literal verbatim re-copy would have erased the ref-forwarding props (`React.RefAttributes<HTMLInputElement>` intersection and `ref` destructure) and broken those consumers.
- **Fix:** Diffed each existing file against the registry source. Confirmed two are byte-identical and the third differs only by the additive ref-forwarding enhancement. Left HEAD intact, ran the plan's verification (typecheck + `npm run verify`) to confirm acceptance criteria are met.
- **Files modified:** None.
- **Verification:** `diff` showed only additive changes on `search-input-pill.tsx`; `npm run typecheck` exits 0; `npm run verify` exits 0 (506 unit tests + 106 RTL tests pass).
- **Committed in:** N/A (no working-tree changes).

---

**Total deviations:** 1 — verification-only deviation, no code changes
**Impact on plan:** Acceptance criteria fully satisfied. Avoided regressing Wave 2 consumers.

## Issues Encountered

- None. The work was already in place from an ancestor commit; the executor's job reduced to verification and SUMMARY authorship.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Wave 2 retrofit plans can import all three components from `@/components/ui/*` — already in active use by `TableToolbarSearch`.
- Registry drift note for the design-system maintainer: the registry copy of `search-input-pill.tsx` lags behind the consumer copy (missing the React 19 ref-as-prop intersection). When the registry is next refreshed, port the ref-forwarding enhancement back upstream so future verbatim copies stay aligned.

## Self-Check: PASSED

- FOUND: src/components/ui/search-input-pill.tsx
- FOUND: src/components/ui/data-table-shell.tsx
- FOUND: src/components/ui/circular-pagination.tsx
- FOUND: ancestor commit `1b886ad` introduced the files (verified via `git log --all`)
- FOUND: ancestor commit `d72daf9` applied the Wave 2 ref-forwarding enhancement (verified via `git log --all`)
- All acceptance criteria from the plan satisfied (named exports present, typecheck clean, `npm run verify` exits 0)

---
*Phase: 01.5-sandra-design-system-retrofit*
*Completed: 2026-05-04*
