---
phase: 01.5-sandra-design-system-retrofit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/search-input-pill.tsx
  - src/components/ui/data-table-shell.tsx
  - src/components/ui/circular-pagination.tsx
autonomous: true
requirements:
  - DS-05

must_haves:
  truths:
    - "SearchInputPill component exists in src/components/ui/"
    - "DataTableShell and DataTableFooter are exported from src/components/ui/data-table-shell.tsx"
    - "CircularPagination component exists in src/components/ui/"
    - "All three files compile without TypeScript errors"
    - "npm run verify passes"
  artifacts:
    - path: "src/components/ui/search-input-pill.tsx"
      provides: "SearchInputPill component"
      exports: ["SearchInputPill", "SearchInputPillProps"]
    - path: "src/components/ui/data-table-shell.tsx"
      provides: "DataTableShell + DataTableFooter components"
      exports: ["DataTableShell", "DataTableFooter"]
    - path: "src/components/ui/circular-pagination.tsx"
      provides: "CircularPagination component"
      exports: ["CircularPagination", "CircularPaginationProps"]
  key_links:
    - from: "src/components/ui/search-input-pill.tsx"
      to: "@/lib/utils"
      via: "import { cn }"
      pattern: 'import.*cn.*@/lib/utils'
---

<objective>
Copy the 3 registry component files from the sibling design system repo into Sandra CRM's ui component directory.

Purpose: Makes SearchInputPill, DataTableShell, DataTableFooter, and CircularPagination importable from @/components/ui/ so that Wave 2 plans can use them. These files are copied verbatim — no npx shadcn add, no modification (per D-03). This plan can run in parallel with Plan 01.5-01 because it touches only new files with no deps on the token wiring.

Output:
- src/components/ui/search-input-pill.tsx (copied from registry)
- src/components/ui/data-table-shell.tsx (copied from registry)
- src/components/ui/circular-pagination.tsx (copied from registry)
</objective>

<execution_context>
@/Users/jarradhenry/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jarradhenry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.5-sandra-design-system-retrofit/01.5-CONTEXT.md
</context>

<interfaces>
<!-- Source files to copy verbatim — read these before creating the destination files -->

From ../Sandra Design System/registry/search-input-pill/search-input-pill.tsx:
```typescript
import { Search } from "lucide-react";
import { cn } from "@/lib/utils";

export interface SearchInputPillProps
  extends Omit<React.InputHTMLAttributes<HTMLInputElement>, "className"> {
  wrapperClassName?: string;
  iconClassName?: string;
}

export function SearchInputPill({
  wrapperClassName, iconClassName, type = "text", ...props
}: SearchInputPillProps) { ... }
```

From ../Sandra Design System/registry/data-table-shell/data-table-shell.tsx:
```typescript
import { cn } from "@/lib/utils";

export interface DataTableShellProps extends React.HTMLAttributes<HTMLDivElement> {}
export function DataTableShell({ className, children, ...props }: DataTableShellProps) { ... }

export interface DataTableFooterProps extends React.HTMLAttributes<HTMLDivElement> {}
export function DataTableFooter({ className, children, ...props }: DataTableFooterProps) { ... }
```

From ../Sandra Design System/registry/circular-pagination/circular-pagination.tsx:
```typescript
import { ChevronLeft, ChevronRight } from "lucide-react";
import { cn } from "@/lib/utils";

export interface CircularPaginationProps
  extends Omit<React.HTMLAttributes<HTMLElement>, "onChange"> {
  currentPage: number;
  totalPages: number;
  onPageChange: (page: number) => void;
  siblingCount?: number;
}

export function CircularPagination({
  currentPage, totalPages, onPageChange, siblingCount = 1, className, ...props
}: CircularPaginationProps) { ... }
```

Note: All three files reference `@/lib/utils` for `cn`. This alias is already configured in Sandra CRM's tsconfig.json — no changes needed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Copy all 3 registry component files into src/components/ui/</name>
  <files>
    src/components/ui/search-input-pill.tsx,
    src/components/ui/data-table-shell.tsx,
    src/components/ui/circular-pagination.tsx
  </files>
  <read_first>
    - ../Sandra Design System/registry/search-input-pill/search-input-pill.tsx — copy source
    - ../Sandra Design System/registry/data-table-shell/data-table-shell.tsx — copy source
    - ../Sandra Design System/registry/circular-pagination/circular-pagination.tsx — copy source
    - src/components/ui/ — list directory to confirm no name collisions before writing
  </read_first>
  <action>
    Copy each registry file verbatim (per D-03). Do NOT use `npx shadcn add` — copy files directly.

    Use the Bash cp command to copy each file:

    ```bash
    cp "../Sandra Design System/registry/search-input-pill/search-input-pill.tsx" \
       "src/components/ui/search-input-pill.tsx"

    cp "../Sandra Design System/registry/data-table-shell/data-table-shell.tsx" \
       "src/components/ui/data-table-shell.tsx"

    cp "../Sandra Design System/registry/circular-pagination/circular-pagination.tsx" \
       "src/components/ui/circular-pagination.tsx"
    ```

    After copying, confirm each file exists and is non-empty:

    ```bash
    wc -l src/components/ui/search-input-pill.tsx \
           src/components/ui/data-table-shell.tsx \
           src/components/ui/circular-pagination.tsx
    ```

    No modifications to the copied files — the `@/lib/utils` import alias already resolves in Sandra CRM.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - ls src/components/ui/search-input-pill.tsx exits 0
    - ls src/components/ui/data-table-shell.tsx exits 0
    - ls src/components/ui/circular-pagination.tsx exits 0
    - grep -c 'export function SearchInputPill' src/components/ui/search-input-pill.tsx outputs 1
    - grep -c 'export function DataTableShell' src/components/ui/data-table-shell.tsx outputs 1
    - grep -c 'export function DataTableFooter' src/components/ui/data-table-shell.tsx outputs 1
    - grep -c 'export function CircularPagination' src/components/ui/circular-pagination.tsx outputs 1
    - npm run verify exits 0 (typecheck passes — @/lib/utils resolves correctly)
  </acceptance_criteria>
  <done>Three registry components exist in src/components/ui/ and compile cleanly with the rest of the codebase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling repo → codebase | Files are copied from a sibling project on the same machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.5-02-01 | Tampering | copied registry files | accept | files are from a controlled sibling repo on the same developer machine; no untrusted source |
| T-01.5-02-02 | Information Disclosure | component files | accept | purely presentational components; no secrets, no PII |
</threat_model>

<verification>
1. `ls src/components/ui/search-input-pill.tsx src/components/ui/data-table-shell.tsx src/components/ui/circular-pagination.tsx` — all three exit 0
2. `grep 'export function' src/components/ui/search-input-pill.tsx src/components/ui/data-table-shell.tsx src/components/ui/circular-pagination.tsx` — shows SearchInputPill, DataTableShell, DataTableFooter, CircularPagination
3. `npm run verify` — exits 0
</verification>

<success_criteria>
- All three component files exist in src/components/ui/
- Each exports the expected named function(s)
- `npm run verify` passes (no TypeScript errors from the copies)
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-sandra-design-system-retrofit/01.5-02-SUMMARY.md`
</output>
