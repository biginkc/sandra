---
phase: 01.5-sandra-design-system-retrofit
plan: 03
subsystem: ui

tags: [shadcn, design-system, table-toolbar, search-input-pill, ref-as-prop, react-19]

# Dependency graph
requires:
  - phase: 01.5-sandra-design-system-retrofit
    provides: SearchInputPill component at @/components/ui/search-input-pill (from plan 01.5-02)
provides:
  - TableToolbarSearch internals now backed by SearchInputPill (registry primitive) instead of bare shadcn Input + manual lucide Search icon
affects:
  - all consumer routes that use <TableToolbarSearch /> (currently /properties, /lists, /jobs, /templates) — visual unchanged, internals unified

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Registry primitive consumption: TableToolbarSearch wraps SearchInputPill, keeping the X clear-button as an outer-wrapper concern (per D-04 — SearchInputPill has no built-in clear)"
    - "React 19 ref-as-prop forwarding through SearchInputPillProps' RefAttributes intersection (consumed via inputRef)"

key-files:
  created: []
  modified:
    - src/components/table/table-toolbar.tsx

key-decisions:
  - "Did not re-apply Task 1 — the plan's intended swap was already committed in worktree-base ancestor `d72daf9 feat(01.5-wave2-03): swap TableToolbarSearch inner input → SearchInputPill`. Re-doing the work would either duplicate the change or revert/re-add it pointlessly."
  - "Did not add `wrapperClassName=\"w-full\"` from the plan's literal action snippet to the existing call site — SearchInputPill's default wrapper already applies `w-full` via `cn(\"relative w-full\", wrapperClassName)`. Adding the prop would be redundant; omitting it is functionally equivalent. The current ancestor commit chose the smaller diff. (Treating this as a deferred-style cleanup — see Deviations.)"

patterns-established:
  - "When a plan's work is already present in an ancestor commit reachable from the worktree base, the executor verifies acceptance criteria, writes SUMMARY.md, and skips re-creating code commits — same pattern used by plan 01.5-02 in this wave"

requirements-completed: [DS-02, DS-05]

# Metrics
duration: 4min
completed: 2026-05-04
---

# Phase 01.5 Plan 03: Retrofit TableToolbarSearch → SearchInputPill Summary

**Inner input primitive of TableToolbarSearch now uses the registry's SearchInputPill — the bare shadcn Input + manual lucide Search icon are gone. External call signature unchanged.**

## Performance

- **Duration:** ~4 min (verification only — code already in place at ancestor commit)
- **Started:** 2026-05-05T19:36:00Z
- **Completed:** 2026-05-05T19:40:00Z
- **Tasks:** 1 (verified, no new code commit)
- **Files modified:** 0 in this plan (the swap was applied in ancestor commit `d72daf9`, reachable from the worktree base `c26ff33`)

## Accomplishments

- Verified `src/components/table/table-toolbar.tsx` uses `SearchInputPill` (2 occurrences — import + usage)
- Verified the bare `Input` import from `@/components/ui/input` is gone (0 occurrences)
- Verified the lucide `Search` import is gone (0 occurrences); the only remaining lucide import is `X`
- Verified the X clear button outer `<button>` is preserved exactly with its absolute positioning (`absolute top-1/2 right-3`) — it floats over the pill input as required by D-04
- Verified `<TableToolbarSearch>` external prop signature is unchanged: `ariaLabel`, `placeholder`, `testId`, `className`
- Verified `aria-label`, `data-testid`, `defaultValue`, `onChange` are forwarded via `...props` spread on SearchInputPill's inner `<input>` so all 6 RTL test cases pass without test modification
- Verified `src/components/table/table-toolbar.test.tsx` has not been modified since its original Phase 01 plan 01-02 commit (`9fcb38f`) — acceptance criterion "All existing RTL tests pass without modification" is satisfied
- `npm run verify` passes: typecheck clean + 41 unit test files / 506 tests + 16 RTL test files / 106 tests (including all 11 in `table-toolbar.test.tsx`)

## Task Commits

| Task | Name                                                                          | Commit                                | Files                                  |
| ---- | ----------------------------------------------------------------------------- | ------------------------------------- | -------------------------------------- |
| 1    | Replace bare Input + Search icon with SearchInputPill in TableToolbarSearch   | `d72daf9` (ancestor — pre-existing)   | `src/components/table/table-toolbar.tsx` |

**Plan metadata:** committed by orchestrator after wave merge.

## Files Created/Modified

- `src/components/table/table-toolbar.tsx` — **already in target state at HEAD via ancestor commit `d72daf9`**:
  - Imports: `import { X } from "lucide-react"` (Search removed); `import { SearchInputPill } from "@/components/ui/search-input-pill"` (replaces shadcn Input)
  - `TableToolbarSearch` body: outer `<div className={cn("relative max-w-md flex-1", className)}>` wrapper unchanged; inner content is `<SearchInputPill ref={inputRef} defaultValue={ctx.search} onChange={…} placeholder={placeholder} aria-label={ariaLabel} data-testid={testId} />` followed by the conditional `<button>` X clear button — exactly as the plan specified

## Decisions Made

- **Did not re-overwrite the file from the plan's literal action snippet.** HEAD already satisfies every acceptance criterion. Re-applying the change would be a no-op or worse, and would not produce a meaningful task commit.
- **Did not add `wrapperClassName="w-full"`.** The plan's action snippet includes it, but `SearchInputPill`'s own wrapper already resolves to `cn("relative w-full", wrapperClassName)` — `w-full` is the default, so passing it explicitly is redundant. The ancestor commit's smaller diff is functionally equivalent and the current `npm run verify` pass confirms behavior is correct.

## Deviations from Plan

### Resolved without code changes

**1. [Rule 1 — Avoid regression] Plan's Task 1 already committed in ancestor `d72daf9`; verification-only execution**

- **Found during:** Task 1 — pre-write read of `src/components/table/table-toolbar.tsx`
- **Issue:** The plan's `<action>` block describes the exact import swap and JSX replacement that was already applied in worktree-base ancestor `d72daf9 feat(01.5-wave2-03): swap TableToolbarSearch inner input → SearchInputPill`. Re-running the changes would either be a no-op (same diff applied) or, worse, would diverge from the ancestor's smaller correct diff.
- **Fix:** Verified each acceptance criterion individually against the file at HEAD, ran `npm run verify` (passes), confirmed `src/components/table/table-toolbar.test.tsx` is unmodified since its original commit. No new code commit was created for Task 1; only the SUMMARY.md commit lands in this plan.
- **Files modified:** None.
- **Verification:**
  - `grep -c 'SearchInputPill' src/components/table/table-toolbar.tsx` → 2 ✅
  - `grep -c 'import.*Input.*@/components/ui/input' src/components/table/table-toolbar.tsx` → 0 ✅
  - `grep -c 'import.*Search.*lucide' src/components/table/table-toolbar.tsx` → 0 ✅
  - `grep -c 'aria-label="Clear search"' src/components/table/table-toolbar.tsx` → 1 ✅
  - `npm run verify` → exit 0 (506 unit + 106 RTL tests pass) ✅

**2. [Note — non-deviation] `grep -c '<Search'` returns 1 not 0**

- **Found during:** Acceptance criterion check
- **Note:** The plan's literal acceptance check `grep -c '<Search' src/components/table/table-toolbar.tsx outputs 0` returns 1, but the match is `<SearchInputPill` (line 96), not the lucide `<Search />` icon. The substantive criterion — "Lucide Search JSX element removed" — IS satisfied (no lucide Search icon in the file). The grep pattern itself is too loose; this is a flaw in the criterion's regex, not in the code. Recording here for transparency rather than as a real deviation. The remaining acceptance criteria pass cleanly.
- **Files modified:** None.

**3. [Note — non-deviation] `wrapperClassName="w-full"` from the plan snippet is omitted at the call site**

- **Found during:** Task 1 verification
- **Note:** The plan's action snippet shows `wrapperClassName="w-full"` on the `<SearchInputPill>` call inside `TableToolbarSearch`. The ancestor commit `d72daf9` omitted this prop. Since `SearchInputPill`'s default wrapper class is `cn("relative w-full", wrapperClassName)`, `w-full` is already applied with or without the prop. Functionally equivalent; the smaller diff is preferable. Recording here so the registry-side audit can spot it if needed.
- **Files modified:** None.

---

**Total deviations:** 1 verification-only (work pre-applied in ancestor) + 2 informational notes (no code change)
**Impact on plan:** Acceptance criteria fully satisfied. No call-site changes needed elsewhere in the codebase.

## Authentication Gates

None.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or trust boundary changes were introduced. Both threat-register entries (T-01.5-03-01 information disclosure on search input, T-01.5-03-02 tampering on props spread) carry `accept` dispositions and remain unchanged. The `...props` spread pattern on `SearchInputPill`'s inner `<input>` is a standard React forwarding pattern; `aria-label` and `data-testid` are presentational only.

## Known Stubs

None — the swap is internals-only. The visible search affordance (pill input with leading magnifying glass + X clear button) renders identically before and after.

## Next Phase Readiness

- All consumer routes using `<TableToolbarSearch>` (currently `/properties`, `/lists`, `/jobs`, `/templates`) keep working with no call-site changes — the external prop signature (`ariaLabel`, `placeholder`, `testId`, `className`) is unchanged
- Future tables retrofitted onto `TableToolbarSearch` automatically inherit the pill-shaped registry primitive without any extra wiring
- DS-02 (TableToolbarSearch retrofit) and DS-05 (registry primitive consumption) are now satisfied

## Self-Check: PASSED

Files verified to exist:
- FOUND: `src/components/table/table-toolbar.tsx` (in target state — uses SearchInputPill, no shadcn Input, no lucide Search)
- FOUND: `src/components/ui/search-input-pill.tsx` (dependency from plan 01.5-02)
- FOUND: `src/components/table/table-toolbar.test.tsx` (unmodified since original commit `9fcb38f`)

Commits verified to exist:
- FOUND: `d72daf9` (ancestor — Task 1: SearchInputPill swap; reachable from HEAD `c26ff33`)
- FOUND: `c26ff33` (worktree base; HEAD before this plan's SUMMARY commit)

Verification commands run:
- `git merge-base --is-ancestor d72daf9 HEAD` → 0 (ancestor confirmed)
- `npm run verify` → 0 (typecheck + 506 unit + 106 RTL all green)

---
*Phase: 01.5-sandra-design-system-retrofit*
*Completed: 2026-05-04*
