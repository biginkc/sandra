---
phase: 01.5-sandra-design-system-retrofit
plan: 01
subsystem: design-system
tags:
  - design-tokens
  - css
  - tailwind-v4
  - sibling-package
requirements:
  - DS-01
  - DS-05
dependency_graph:
  requires: []
  provides:
    - "@sandra/tokens/theme.css imported into globals.css"
    - "node_modules/@sandra/tokens symlink materialized via npm file: reference"
    - "warm-paper :root tokens sourced from sibling repo (single source of truth)"
  affects:
    - "all routes consuming Tailwind utilities backed by --background, --foreground, --primary, --border, --radius, --sidebar-* (every dashboard surface)"
    - "Wave 2 plans depend on this — they assume tokens come from @sandra/tokens, not from globals.css"
tech_stack:
  added:
    - "@sandra/tokens (file:../Sandra Design System) — internal sibling package"
  patterns:
    - "npm file: reference for sibling-repo packages"
    - "@import \"@sandra/tokens/theme.css\" at the top of globals.css to source CSS variables"
key_files:
  created: []
  modified:
    - "package.json"
    - "package-lock.json"
    - "src/app/globals.css"
decisions:
  - "Use npm `file:` protocol for the sibling-repo design-token package (not workspaces, not a published package). Matches the plan's must_have artifact spec and keeps the dependency local-only."
  - "Place the @sandra/tokens import on line 4 (immediately after shadcn/tailwind.css). This puts shared tokens after Tailwind layer setup but before the @theme inline{} block, so token values resolve when @theme reads var(--background) etc."
  - "Remove BOTH the local :root{} block AND the design-refresh comment above it. The shared theme.css is now the documentation source; keeping a stale comment in globals.css would invite drift."
  - "Retain .dark{} and @layer base{} in globals.css. .dark is not yet in the sibling theme package as :root-level overrides only; @layer base contains app-specific apply() rules that aren't tokens."
metrics:
  duration_minutes: 4
  task_count: 2
  files_touched: 3
  tests_run: 612
  tests_passed: 612
  completed_date: "2026-05-04"
---

# Phase 01.5 Plan 01: Wire @sandra/tokens — Summary

Establishes the shared design-token package as the single source of truth for warm-paper visual tokens, replacing the in-file `:root{}` block in globals.css with an `@import` from the sibling `Sandra Design System` repo via npm `file:` reference. Prerequisite for all Wave 2 plans in this phase.

## What Shipped

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Add @sandra/tokens to package.json and install | `6235f07` | package.json, package-lock.json |
| 2 | Update globals.css — add import, remove :root block | `7e7062b` | src/app/globals.css |

### Task 1 — package wiring
- Added `"@sandra/tokens": "file:../Sandra Design System"` to dependencies, alphabetised between `@dnd-kit/utilities` and `@supabase/ssr`
- `npm install` materialized `node_modules/@sandra/tokens` as a symlink to `../Sandra Design System`
- Verified `node_modules/@sandra/tokens/tokens/theme.css` is reachable

### Task 2 — globals.css token import
- Inserted `@import "@sandra/tokens/theme.css";` on line 4 (after `shadcn/tailwind.css`)
- Removed the local `:root { ... }` warm-paper block (was lines 70–103) — now sourced from the sibling theme.css
- Removed the design-refresh comment block above it (was lines 56–68); the shared theme.css README owns this documentation now
- Retained `@theme inline {}`, `.dark {}`, and `@layer base {}` (none of those are tokens; they're app-specific or not yet in the sibling package)

## Verification

| Check | Expected | Actual |
| ----- | -------- | ------ |
| `grep '"@sandra/tokens": "file:../Sandra Design System"' package.json` | exit 0 | exit 0 |
| `ls node_modules/@sandra/tokens/tokens/theme.css` | exit 0 | exit 0 (resolves through symlink) |
| `grep -c '@import "@sandra/tokens/theme.css"' src/app/globals.css` | 1 | 1 |
| `grep -n ':root' src/app/globals.css` | no matches | no matches |
| `grep -n 'fdfcfb' src/app/globals.css` | no matches | no matches |
| `npm run verify` (typecheck + 506 unit + 106 RTL) | exit 0 | exit 0 |

Total tests passed: **612 / 612**.

## Deviations from Plan

### [Rule 3 — Blocking issue] Worktree-relative path for `file:../Sandra Design System`

- **Found during:** Task 1, before `npm install`
- **Issue:** The plan declares the file: reference as `file:../Sandra Design System`. From the main repo at `/Users/jarradhenry/Sites/Sandra/`, that resolves to `/Users/jarradhenry/Sites/Sandra Design System/` — correct. From this worktree at `/Users/jarradhenry/Sites/Sandra/.claude/worktrees/agent-a0baf05a98bd99554/`, that resolves to `/Users/jarradhenry/Sites/Sandra/.claude/worktrees/Sandra Design System/` — which does not exist. `npm install` would fail in the worktree.
- **Fix:** Created a symlink at `/Users/jarradhenry/Sites/Sandra/.claude/worktrees/Sandra Design System` pointing to `/Users/jarradhenry/Sites/Sandra Design System`. The symlink lives under `.claude/worktrees/` (not part of the repo) so it is not committed and does not pollute the repository. Once the worktree branch is merged into main and tooling runs from the main repo, the original `file:../Sandra Design System` resolves correctly without needing the symlink.
- **Files modified:** None (filesystem-only setup outside the repo)
- **Plan content unchanged:** package.json still contains the exact must_have string `"@sandra/tokens": "file:../Sandra Design System"`.

No other deviations. Both tasks completed exactly as specified.

## Authentication Gates

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or trust boundary changes were introduced. The threat register's two `accept` dispositions (sibling-repo symlink trust + npm install DoS) remain unchanged.

## Known Stubs

None — no UI surfaces were added or modified. The token swap is invisible at the value level (the sibling theme.css carries the same warm-paper hex values that the local `:root{}` block had).

## Self-Check: PASSED

Files verified to exist:
- FOUND: `package.json` (with `"@sandra/tokens": "file:../Sandra Design System"`)
- FOUND: `package-lock.json` (updated)
- FOUND: `src/app/globals.css` (line 4 = `@import "@sandra/tokens/theme.css";`)
- FOUND: `node_modules/@sandra/tokens/tokens/theme.css` (through symlink)

Commits verified to exist:
- FOUND: `6235f07` (Task 1)
- FOUND: `7e7062b` (Task 2)
