# Inbox outcome facets

Depends on performance PR575. Private disposable-fixture implementation; no production activation or migration. Source scope is separate from work-filter counters.

The authenticated scalar public.inbox_outcome_counts_v1(org_id,filter) accepts the existing strict normalized filter. It returns outcome_counts, known_total, unknown_count, as_of, access_epoch and explicit semantics. Known counts cover recent known conversations, apply search and hide_noise, and always describe the all view regardless of selected work filter. Counts measure conversations, not distinct prospects/properties. Unknown_count covers active unknown senders and ignores known search exactly as legacy. The9 legal display outcomes include callback_requested and booked_appointment; no_outcome represents null. Known outcomes are zero-filled. Future canonical values are preserved rather than silently discarded.

Metadata stores raw maintained outreach_dispo in the existing typed row; trigger writes and locked backfill serialize on source rows. No source values are rewritten. The aggregation groups one filtered scan; it does not make nine independent count queries. The endpoint reauthorizes and compares user/session/epoch before returning.

Actual fixture test.py passed all9 outcomes, no-outcome conservation, search, hide-noise, unknown isolation, cross-org and anon/service denial. The existing14 canonical bridge checks plus cursor/search suites passed as prerequisites. Scale proof independently checked known/unknown totals at120k/360k;10-sample maxima90.099/229.603ms on512MiB1CPU. These are synthetic server times, not production latency acceptance. Actual sample response is evidence.json →sample.

To reproduce after the reviewed bridge/performance fixture install, obtain exclusive disposable T2 mutation ownership and run install.py --run-owned-fixture. It validates container/marker, refuses duplicate setup, installs setup.sql and backfills1000 source rows per transaction under FOR SHARE. Actual run examined480044 rows. Then run test.py and scale-proof.py with the same explicit flag. No production DDL. verify.py --installed is read-only and verifies three effective candidate function bodies. This child intentionally overrides the earlier upsert_filter definition; use this child's final-source verifier after installation rather than claiming the old parent's body still matches.

Authentication production feasibility has separate schema-only evidence and an explicit proposal. Actual production postgres role has SELECT/TRIGGER on auth.sessions despite SupabaseAuth ownership; no production Auth ACL/RLS/DDL changed. Role-equivalent rollback-only fixture proof passed: nonowner postgres created the trigger; Auth-role INSERT fired the definer private write; direct DROP TRIGGER was denied; dropping the postgres-owned function CASCADE removed it; full rollback left no probe schema. Production rollback must use this verified ownership-compatible route. Full canonical source→worker outcome capture is inherited dependency behavior, not newly proven by these maintained-row tests.
