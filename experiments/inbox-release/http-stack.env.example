# Copy to an ignored local env file before starting the disposable fixture.
# Never commit the populated file. Values below are intentionally absent.
HTTP_DB_PASSWORD=
HTTP_JWT_SECRET=
HTTP_ANON_KEY=
HTTP_SERVICE_ROLE_KEY=
HTTP_OPERATOR_TOKEN=
HTTP_AUTH_DB_URL=
HTTP_REST_DB_URI=
HTTP_REALTIME_DB_PASSWORD=
HTTP_REALTIME_SECRET_KEY_BASE=
HTTP_REALTIME_DB_ENC_KEY=
HTTP_PROJECTION_ENV_FILE=../../work/inbox-http-projection.env
